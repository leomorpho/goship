---
id: TKT-289
seq: 289
state: in-review
priority: 2
labels:
    - feature
    - test
    - quality
    - topo:leaf
    - topo:lane-goship
    - topo:wave-01
created_at: "2026-03-18T14:24:36Z"
updated_at: "2026-03-20T05:20:00Z"
created_by: human:Leo Audibert
write_hash: d430482ee58c600fcfc4f191bc139cfffb6f265e7d770cf914ce33f57b8211ee
---

# TKT-289: G6-02-A: Define executable spec and red tests

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-289` before committing. -->

## Description
Atomic child A for TKT-211 (G6-02: Define module compatibility/version policy and enforce via verify): define exact behavior and create failing tests first.

Likely paths:
- .docket/tickets/TKT-211.md
- docs/architecture/06-known-gaps-and-risks.md
- docs/roadmap/01-framework-plan.md

Verify commands:
- docket validate TKT-211
- docket validate $TICKET_ID

Out of scope:
- Shipping full implementation.
- Deferring failure modes to follow-up tickets.

## Acceptance Criteria
- [x] Implementation scope is complete and constrained to this ticket's described boundaries. : Current HEAD already carries the module-compatibility red-spec through `tools/cli/ship/internal/commands/verify_test.go`, which pins `ship verify` failures when standalone-module wiring drifts from the local-workspace policy.
- [x] Automated tests and verification commands for this scope pass or documented blockers are explicit. : Verified at current HEAD with `go test ./tools/cli/ship/internal/commands -run TestRunVerify -count=1` plus `docket validate TKT-211` and `docket validate TKT-289`.
- [x] Relevant docs/contracts are updated with no unmanaged scope expansion. : The live contract is already described in `docs/reference/01-cli.md` and `docs/architecture/06-known-gaps-and-risks.md`, including the `v0.0.0` + local `replace` + matching `go.work use` policy.

## Comments

### 2026-03-20T05:20:00Z — codex
Confirmed the G6-02 red-spec leaf is satisfied at current HEAD. The executable contract is the `ship verify` module-compatibility step plus the focused `verify_test.go` drift case that requires local standalone batteries to stay on `v0.0.0` with repo-local `replace` and matching `go.work use`.

## Handoff
Current state
- The module-compatibility red-spec is present and review-ready at current HEAD.
- Current HEAD keeps the `ship verify` contract and docs aligned for local-workspace versus tagged-release module wiring.

Decisions made
- Kept this leaf scoped to the verification contract instead of reworking module installation flows.
- Used the existing `ship verify` step and its focused failure case as the executable boundary for the red spec.

Files touched
- .docket/tickets/TKT-289.md
- tools/cli/ship/internal/commands/verify_test.go
- docs/reference/01-cli.md
- docs/architecture/06-known-gaps-and-risks.md

Remaining work
- Review the recorded evidence and keep the module-compatibility docs/tests aligned if the policy changes.
- TKT-290 remains the implementation/doc-sync follow-on for the broader parent stream.

AC status
- [x] Spec and contract evidence is recorded if this ticket is worked.
- [x] Any uncovered contract gaps are documented for the parent scope.
