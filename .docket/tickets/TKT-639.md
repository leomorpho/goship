---
id: TKT-639
seq: 639
state: in-progress
priority: 3
labels:
    - refactor
    - cleanup
parent: TKT-635
created_at: "2026-03-26T16:34:12Z"
updated_at: "2026-03-26T17:13:57Z"
started_at: "2026-03-26T17:11:23Z"
created_by: human:Leo Audibert
write_hash: c625c39e2e6bd7cbd80a4a83fe0a5f0d5b362bc801a57c8075d6513e13d78fa0
---

# TKT-639: audit and clean up root-level Go files in goship/

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-639` before committing. -->

## Description
Audit and fix specific known issues in root-level Go files at goship/.

Known issues to fix (all confirmed via code read):

1. docker_image_smoke_test.go — package declaration is `package foundation` but should match the root package. Build-tagged `integration` so it does not break normal builds, but the mismatch is wrong. Fix the declaration.

2. router.go lines 68-70 — incomplete API v1 wiring. `v1 := e.Group("/api/v1")` is created then immediately discarded with `_ = v1`. Either populate with routes or remove the placeholder.

3. testConfigForDevelop() / testConfigForProduction() — defined identically in both router_mail_preview_test.go and startup_contract_test.go. Extract to a shared test helpers file (e.g. testhelpers_test.go at root).

4. doombox.json.bak — backup file committed to root. Delete or gitignore.

Files confirmed clean (no action needed):
- container.go, schedules.go
- container_test.go, container_adapters_test.go
- router_contract_test.go, router_guardrails_test.go, schedules_test.go
- startup_contract_test.go (except testConfigForDevelop duplication per item 3)

Likely paths:
- docker_image_smoke_test.go
- router.go (lines 68-70)
- router_mail_preview_test.go + startup_contract_test.go → new testhelpers_test.go
- doombox.json.bak

Out of scope: framework or module code, adding new routes to the v1 group.

Verify commands:
- go build ./...
- go test ./...
- go test -tags integration ./...

## Acceptance Criteria
- [x] each root-level Go file is either justified in place or moved/deleted : Root go surfaces audited: container/router/schedules and root tests remain in place and are justified; stray doombox backup removed.
- [x] no orphaned TODOs remain in root-level files : rg -n TODO *.go returns no matches in goship root-level Go files.
- [x] root-level test files cover what they claim to cover : Root tests cover container/router/schedules/startup/mail-preview/docker-smoke contracts (17 Test* funcs across *_test.go root files).
- [ ] go build ./... passes
- [ ] go test ./... passes
- [x] docker_image_smoke_test.go package declaration corrected : docker_image_smoke_test.go package changed from foundation to goship under integration build tag.
- [x] router.go v1 group placeholder removed or populated — no dangling _ = v1 : Removed dangling v1 placeholder assignment in router.go; no _ = v1 remains.
- [x] testConfigForDevelop() and testConfigForProduction() extracted to shared helpers file, duplicates removed : Created testhelpers_test.go with testConfigForDevelop/testConfigForProduction and removed duplicate definitions from router_mail_preview_test.go.
- [x] doombox.json.bak removed from repo : Deleted root backup artifact doombox.json.bak from repo.
- [ ] go build ./... passes
- [ ] go test ./... passes

## Handoff
Implementation complete for scoped cleanup items (AC 1,2,3,6,7,8,9). Verification commands (go build ./..., go test ./..., go test -tags integration ./...) are currently blocked by local disk exhaustion: /System/Volumes/Data at 100% with ~436MiB free. Re-run verification after freeing disk, then complete AC 4,5,10,11 and close.
