---
id: TKT-616
seq: 616
state: in-review
priority: 4
labels:
    - feature
    - test
    - roadmap:production-system
    - topo:leaf
    - topo:lane-goship
parent: TKT-608
created_at: "2026-03-25T17:06:21Z"
updated_at: "2026-03-25T18:17:37Z"
started_at: "2026-03-25T18:10:51Z"
created_by: human:Leo Audibert
write_hash: b2db7810bdfb22057b4dc49138b83e4ed6a6f9e69d458a1784e451651f4dfb81
---

# TKT-616: Make ship-mcp execute the checked-out ship CLI instead of relying on a stale PATH binary.

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-616` before committing. -->

## Description
Objective: Make ship-mcp execute the checked-out ship CLI instead of relying on a stale PATH binary.

Context:
The current MCP server shells out to `ship` on PATH. That breaks the core LLM promise because the globally installed binary can drift from the checked-out repo and advertise removed or stale command surfaces.

Rails/Laravel DX bar:
- LLM tooling must reflect the current checkout, not ambient machine state.
- If the repo provides an MCP server, it should be more reliable than copy-pasting shell commands.

Likely paths:
- tools/mcp/ship/internal/server/tools_ship.go
- tools/mcp/ship/internal/server/tools.go
- tools/mcp/ship/internal/server/server_test.go
- tools/mcp/ship/internal/server/tools_test.go
- tools/mcp/ship/README.md

Likely symbols:
- lookPathShip
- runShipJSON
- runShipDescribePayload
- repo-root CLI resolution

Tests to add or update:
- MCP server tests proving repo-local CLI resolution wins over PATH drift.
- Failure-mode coverage for missing repo-local CLI/module-path execution.

Verify commands:
- docket validate TKT-612
- go test ./tools/mcp/ship/... -count=1
- go run ./tools/mcp/ship/cmd/ship-mcp

Out of scope:
- Adding new MCP tools.
- Rewriting MCP docs beyond what is required to explain the new resolution model.
- Changing the GoShip CLI surface itself.

## Acceptance Criteria
- [x] Code changes are limited to the described scope. : Changes scoped to tools/mcp/ship server code/tests and MCP README.
- [x] Automated tests prove repo-local CLI resolution wins over PATH drift. : Added tests TestResolveShipInvocation_UsesRepoLocalCLIOverPath and TestResolveShipInvocation_MissingGoDoesNotFallbackToPath plus updated tool call coverage.
- [x] Verification commands and outcomes are recorded in ticket notes or handoff. : Ticket comments include validate/test/go-run verification outcomes.

## Comments

### 2026-03-25T18:13:50Z — human:Leo Audibert
Implemented repo-local CLI resolution for ship-mcp. The server now prefers  when the checked-out CLI exists, and only falls back to PATH  if the repo-local entrypoint is absent. Added tests covering repo-local precedence over PATH drift and missing-go failure mode without PATH fallback. Verification: docket validate TKT-612; go test ./tools/mcp/ship/... -count=1; go run ./tools/mcp/ship/cmd/ship-mcp </dev/null.

### 2026-03-25T18:13:56Z — human:Leo Audibert
Correction: ship-mcp now resolves CLI execution as follows.
1) Prefer repo-local CLI by executing: go run <repo>/tools/cli/ship/cmd/ship
2) Fallback to PATH `ship` only when repo-local CLI entrypoint is absent.
3) If repo-local CLI exists but `go` toolchain is missing, return an explicit error and do not fallback to PATH.

Verification repeated:
- docket validate TKT-612
- go test ./tools/mcp/ship/... -count=1
- go run ./tools/mcp/ship/cmd/ship-mcp </dev/null

### 2026-03-25T18:15:47Z — human:Leo Audibert
Committed as b8269a0a with Ticket trailer. Pre-commit suite and targeted MCP verification commands passed.

## Handoff
## Current state
- Ticket implemented and committed as `b8269a0a`.
- `ship-mcp` now prefers repo-local CLI execution via `go run tools/cli/ship/cmd/ship`.

## Decisions made
- Prefer repo-local CLI over PATH to prevent stale binary drift.
- Do not fallback to PATH when repo-local CLI exists but `go` is unavailable; fail explicitly.

## Files touched
- tools/mcp/ship/internal/server/tools.go
- tools/mcp/ship/internal/server/tools_ship.go
- tools/mcp/ship/internal/server/tools_test.go
- tools/mcp/ship/README.md

## Remaining work
- None for this ticket.

## AC status
- [x] Code changes are limited to the described scope.
- [x] Automated tests prove repo-local CLI resolution wins over PATH drift.
- [x] Verification commands and outcomes are recorded in ticket notes or handoff.
