---
id: TKT-485
seq: 485
state: in-review
priority: 4
labels:
    - feature
    - test
    - roadmap:production-system
    - topo:leaf
    - topo:lane-goship
parent: TKT-484
blocked_by:
    - TKT-483
created_at: "2026-03-21T16:10:31Z"
updated_at: "2026-03-25T01:49:34Z"
started_at: "2026-03-25T01:38:29Z"
created_by: human:Leo Audibert
write_hash: 74b67db1a56acfa786461f81ee30a80e34397b40c3c182db34b3ecad03e9d50e
---

# TKT-485: Make ship doctor surface upgrade readiness and blockers and prove both human and JSON output.

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-485` before committing. -->

## Description
Objective: Make ship doctor surface upgrade readiness and blockers and prove both human and JSON output.

Context:
These tickets expose upgrade readiness into fleet-level automation. Keep the runtime report and upgrade report aligned so the control plane can trust the data without inference.

Rails/Laravel DX bar:
- Prefer one obvious convention, delete fallback paths, and keep the generated or framework-owned surface beautiful enough to feel like Rails or Laravel.

Likely files:
- tools/cli/ship/internal/commands/project_upgrade.go
- tools/cli/ship/internal/commands/runtime_report.go
- tools/cli/ship/internal/commands/runtime_report_contract_test.go
- tools/cli/ship/internal/commands/project_upgrade_test.go

Likely symbols:
- UpgradeReadinessReport
- RunRuntimeReport
- runtimeReportHandshake

Tests to add or update:
- tools/cli/ship/internal/commands/runtime_report_contract_test.go
- tools/cli/ship/internal/commands/project_upgrade_test.go

Verify commands:
- docket validate TKT-485
- go test ./tools/cli/ship/internal/commands -run "Upgrade|RuntimeReport" -count=1

Out of scope:
- Broad refactors outside the scoped ownership surface for this ticket.
- Writing roadmap docs that are not required to keep code and tests aligned.
- Taking shortcuts that preserve multiple competing conventions instead of choosing one clear path.

## Acceptance Criteria
- [ ] Code changes are limited to the described scope.
- [ ] Automated tests for the changed behavior pass.
- [ ] Verification commands and outcomes are recorded in ticket notes or handoff.

## Comments

### 2026-03-25T01:49:33Z — human:Leo Audibert
Implemented upgrade readiness surfacing in both doctor and runtime report outputs.

What shipped:
- `ship doctor --json` now emits `upgrade_readiness` (`ready`, `blockers`) and exit semantics now include readiness status.
- Human `ship doctor` output now prints an explicit upgrade readiness section (`ready`/`blocked`).
- `ship runtime:report --json` now includes `upgrade_readiness` metadata aligned with the runtime payload.
- Added contract + command tests for readiness schema presence and blocker reporting.
- Updated CLI reference docs and doc-sync contract expectations.

Verification:
- `go test ./tools/cli/ship/internal/policies -run Doctor -count=1`
- `go test ./tools/cli/ship/internal/commands -run "Upgrade|RuntimeReport|Docs_DBExportAndRuntimeReportContractsStayInSync" -count=1`
- `go test ./tools/cli/ship/internal/commands -count=1`
- `make test`
- `DOCKET_HOME=/tmp/docket-home docket validate TKT-485`
- pre-commit (`lefthook`) passed during commit

Commit:
- 171616a7 (`feat(cli): surface upgrade readiness in doctor/runtime report`)

