---
id: TKT-638
seq: 638
state: backlog
priority: 2
labels:
    - refactor
    - cleanup
parent: TKT-635
blocked_by:
    - TKT-637
created_at: "2026-03-26T16:34:01Z"
updated_at: "2026-03-26T16:34:01Z"
created_by: human:Leo Audibert
write_hash: 61f60986e7203d469fc90182a0dc1e52a36aa81d29469f49a0cb1418754f90ef
---

# TKT-638: deduplicate SSEEvent type — defined identically in two packages

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-638` before committing. -->

## Description
SSEEvent struct is defined twice with identical fields (Type string, Data string, same JSON tags):
1. modules/notifications/notifier.go:28
2. framework/repos/pubsub/pubsub.go:13 (will be repos/ssepubsub after TKT-637)

Fix: keep canonical definition in framework/repos/ssepubsub, delete local copy in notifier.go, import from framework. Do after TKT-637 so the import path is stable.

Likely paths:
- modules/notifications/notifier.go (remove local SSEEvent, add import)
- framework/repos/ssepubsub/pubsub.go (canonical definition stays here)

Out of scope: no logic changes.

Verify commands:
- grep -rn 'type SSEEvent' --include='*.go' . (must return exactly 1 result)
- go build ./...
- go test ./...

## Acceptance Criteria
- [ ] SSEEvent defined in exactly one location (framework/repos/ssepubsub)
- [ ] modules/notifications/notifier.go imports SSEEvent from framework package
- [ ] grep for 'type SSEEvent' returns exactly one result
- [ ] go build ./... passes
- [ ] go test ./... passes

