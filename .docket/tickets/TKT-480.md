---
id: TKT-480
seq: 480
state: in-review
priority: 4
labels:
    - feature
    - test
    - roadmap:production-system
    - topo:leaf
    - topo:lane-goship
parent: TKT-476
blocked_by:
    - TKT-479
created_at: "2026-03-21T16:10:29Z"
updated_at: "2026-03-25T00:48:40Z"
started_at: "2026-03-25T00:45:10Z"
created_by: human:Leo Audibert
write_hash: 151a03387e3c79adc65fab7c33ea1e7d688507fc359deabb97db0db0f270d8d8
---

# TKT-480: Add drift detection against canonical generated conventions and prove blocker output in stale fixture apps.

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-480` before committing. -->

## Description
Objective: Add drift detection against canonical generated conventions and prove blocker output in stale fixture apps.

Context:
This is the executable `ship upgrade` flow. Keep file rewrites deterministic, surface manual follow-up clearly, and prefer codemods over hand-wavy guidance.

Rails/Laravel DX bar:
- Prefer one obvious convention, delete fallback paths, and keep the generated or framework-owned surface beautiful enough to feel like Rails or Laravel.

Likely files:
- tools/cli/ship/internal/commands/project_upgrade.go
- tools/cli/ship/internal/commands/project_upgrade_test.go
- tools/cli/ship/internal/commands/upgrade_readiness_contract_test.go
- docs/reference/01-cli.md

Likely symbols:
- RunUpgrade
- RewriteGooseVersion
- upgradeGoose
- buildUpgradeReadinessReport

Tests to add or update:
- tools/cli/ship/internal/commands/project_upgrade_test.go

Verify commands:
- docket validate TKT-480
- go test ./tools/cli/ship/internal/commands -run Upgrade -count=1

Out of scope:
- Broad refactors outside the scoped ownership surface for this ticket.
- Writing roadmap docs that are not required to keep code and tests aligned.
- Taking shortcuts that preserve multiple competing conventions instead of choosing one clear path.

## Acceptance Criteria
- [ ] Code changes are limited to the described scope.
- [ ] Automated tests for the changed behavior pass.
- [ ] Verification commands and outcomes are recorded in ticket notes or handoff.

## Comments

### 2026-03-25T00:48:39Z — human:Leo Audibert
Added upgrade drift detection for stale generated conventions and surfaced blocker output through the readiness contract.

What changed:
- `RewriteGooseVersion` now returns a typed convention-drift error when canonical generated anchors are missing.
- `RunUpgrade --json` now returns a structured blocked readiness report instead of empty/invalid output for stale fixtures.
- Added stale fixture app input and TDD coverage proving `result.outcome=blocked` with `upgrade.convention_drift` blocker metadata.

Verification:
- go test ./tools/cli/ship/internal/commands -run Upgrade -count=1 (pass)
- go test ./tools/cli/ship/internal/commands -count=1 (pass)
- docket validate TKT-480 (pass)
- make test (pass)
- pre-commit lefthook suite (pass)

Commit: 7de54105 (feat(cli): block upgrade on convention drift)

