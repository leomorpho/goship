---
id: TKT-176
seq: 176
state: done
priority: 7
labels:
    - refactor
parent: TKT-170
created_at: "2026-03-17T22:35:36Z"
updated_at: "2026-03-18T02:11:49Z"
started_at: "2026-03-18T01:09:39Z"
completed_at: "2026-03-18T02:11:49Z"
created_by: human:Leo Audibert
write_hash: a8f62529f2c50dfefe8d387bb082da6fd0fe63fc8d23d4d751555a4fac7d0b3e
---

# TKT-176: Decide and implement final ownership for Page type alias usage

## Description
Decide and implement final ownership for `Page` type usage currently routed through `app/controller` alias.

Decision captured:
- `app/controller` is deleted in this cleanup stream.
- Canonical import for current code becomes `app/web/ui`.
- Framework-level extraction remains a follow-up once app-coupled fields are split from reusable page base concerns.

Context:
`app/controller` currently contains only a compatibility alias and adds confusion. We want minimal app shape while avoiding a rushed framework move that drags app-only dependencies into `framework`.

Scope:
- Record the ownership decision in ticket notes/description.
- Migrate Go call sites off `app/controller` to `app/web/ui`.
- Establish follow-up framework extraction ticket for a reusable `framework/web/page` base model.

Out of scope:
- Bulk templ import migration and alias package deletion (handled by `TKT-177`).
- Full framework page model extraction in this ticket.

Likely paths:
- `app/controller/controller.go`
- `app/web/ui/page.go`
- `app/web/controllers/mail_preview.go`
- `framework/repos/mailer/render_test.go`

Verify commands:
- `go test ./app/web/controllers/... ./framework/repos/mailer/...`
- `go test ./...`

## Acceptance Criteria
- [x] Ticket notes explicitly record: delete app/controller now; canonical import is app/web/ui for this stream. : Ticket description records decision: delete app/controller in this stream; canonical import app/web/ui; framework extraction deferred follow-up.
- [x] Go call sites no longer import app/controller. : Migrated remaining non-generated Go call sites (mail_preview.go, render_test.go) to app/web/ui; rg check confirms zero non-generated Go imports of app/controller.
- [x] Follow-up framework extraction ticket is created and linked. : Follow-up extraction ticket TKT-186 exists and is linked as blocked by TKT-177 / downstream of this decision ticket.

## Comments

### 2026-03-18T01:10:27Z — human:Leo Audibert
Implemented in commit 20f5cc4 (refactor(ui): migrate go call sites off app/controller alias). Verified with go test ./app/web/controllers/... ./framework/repos/mailer/... and go test ./...; non-generated Go imports of app/controller are now zero.

