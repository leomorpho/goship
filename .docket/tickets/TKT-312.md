---
id: TKT-312
seq: 312
state: backlog
priority: 3
labels:
    - feature
    - test
    - docs
    - topo:leaf
    - topo:lane-goship
    - topo:wave-02
parent: TKT-249
created_at: "2026-03-18T14:24:54Z"
updated_at: "2026-03-19T04:30:31Z"
created_by: human:Leo Audibert
write_hash: f6bab957b9e20a6d82558afb9955c404cb1b05029e6089eec82e4df0ed8d686b
---

# TKT-312: G1-08-B: Implement, verify, and sync docs

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-312` before committing. -->

## Description
Atomic child B for TKT-249 (G1-08: Hard API and CLI reset to one canonical command path per concern): implement scoped behavior, close tests, and sync docs.

Likely paths:
- .docket/tickets/TKT-249.md
- docs/reference/01-cli.md
- docs/architecture/03-project-scope-analysis.md
- docs/architecture/04-http-routes.md

Verify commands:
- docket validate TKT-249
- docket validate $TICKET_ID
- go test ./...

Out of scope:
- New roadmap scope beyond the parent ticket.
- Broad refactors unrelated to parent acceptance criteria.

## Acceptance Criteria
- [x] Implementation is merged only after parent scope tests pass in targeted packages. : Removed the top-level `ship check` dispatch path, moved the fast package-list loop into `ship test`, and kept targeted CLI/help contract tests green across tools/cli/ship/internal/commands and tools/cli/ship/internal/cli.
- [x] Docs and architecture references are updated in the same change stream when behavior changes. : Updated docs/reference/01-cli.md, docs/roadmap/01-framework-plan.md, docs/architecture/06-known-gaps-and-risks.md, and the generated agent-policy allowlist artifacts to reflect `ship test`/`ship verify` as the canonical quality surface.
- [x] Verification commands and outcomes are recorded in ticket notes for reproducibility. : Verified with go test ./tools/cli/ship/internal/commands -run 'Test(PrintRootHelp_DirectCommandsDiscoverableNoAliases|PrintAdditionalScopedHelp_IncludeDescriptions|CLIReference_UsesCanonicalTopLevelQualityCommands_RedSpec)$' -count=1, go test ./tools/cli/ship/internal/cli -run 'Test(Run_DispatchAndArgs|RunTest_UsesProjectPackageLists|RunTest_FallbackToGoTestAll)$' -count=1, go run ./tools/cli/ship/cmd/ship agent:setup, go run ./tools/cli/ship/cmd/ship agent:check, and docket validate TKT-312.

## Comments

### 2026-03-18T19:18:12Z — human:Leo Audibert
Implemented the CLI quality-path reset. `ship test` now owns the fast package-list loop, `ship verify` remains the full workflow, and the legacy `ship check` path has been removed from dispatch, help text, docs, and agent-policy surfaces.

## Handoff
Current state
- Implementation-ready leaf for the parent scope.
- This ticket should be picked directly when the local write scope is viable.

Decisions made
- Removed the synthetic dependency on the sibling red-spec ticket.
- If targeted failing coverage is missing, add it inside this ticket before implementation.

Files touched
- .docket/tickets/TKT-312.md
- docs/roadmap/01-framework-plan.md

Remaining work
- Implement the scoped behavior, run verification, and sync docs in one change stream.
- Record any true external prerequisite as evidence instead of recreating phase gating.

AC status
- [ ] Ticket can be executed without waiting on sibling roadmap tickets.
- [ ] Verification evidence should be recorded when implementation lands.
