---
id: TKT-589
seq: 589
state: done
priority: 3
labels:
    - feature
    - topo:leaf
    - topo:lane-goship
parent: TKT-580
created_at: "2026-03-24T02:49:13Z"
updated_at: "2026-03-24T15:03:44Z"
started_at: "2026-03-24T14:55:48Z"
completed_at: "2026-03-24T15:02:18Z"
created_by: human:Leo Audibert
write_hash: 9d3eaff008ee51994fc720ea3ec854986a848ad39e16737f4eb88fa78b8e730c
---

# TKT-589: Generate .env.example in ship new with all required and optional env vars documented

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-589` before committing. -->

## Description
When ship new creates a new app, it must emit a .env.example file that documents every env var the app may need: required vars (APP_KEY, DATABASE_URL, etc.), optional vars with defaults (CACHE_DRIVER=memory, QUEUE_DRIVER=backlite), and battery-specific vars injected on ship module:add (STRIPE_KEY, RESEND_API_KEY, etc.). Include inline comments explaining each var. Table stakes for Rails/Laravel parity.

Likely files:
- tools/cli/ship/internal/commands/new.go
- tools/cli/ship/internal/templates/ (new .env.example template)
- tools/cli/ship/internal/commands/module_add.go (inject battery vars on module:add)

Verify commands:
- go test ./tools/cli/ship/... -count=1 -run New
- docket validate $TICKET_ID

Out of scope:
- Secret rotation or vault integration.
- Dynamic env var discovery at runtime.

## Acceptance Criteria
- [ ] ship new myapp generates a .env.example file in the project root
- [ ] .env.example includes all required vars (APP_KEY, DATABASE_URL) with inline comments
- [ ] .env.example includes optional vars with defaults (CACHE_DRIVER=memory, QUEUE_DRIVER=backlite)
- [ ] ship module:add billing appends STRIPE_KEY, STRIPE_WEBHOOK_SECRET to .env.example
- [ ] go test ./tools/cli/ship/... -count=1 -run New passes

## Handoff
Current state
- ship new now emits .env.example with required and optional documented vars.
- ship module:add billing (alias of paidsubscriptions) appends Stripe vars to .env.example idempotently.

Decisions made
- Reused module metadata with EnvExampleSnippet so module:add can extend .env.example without hardcoded per-command branching.
- Added billing as an alias entry that resolves to paidsubscriptions wiring and manifest id.

Files touched
- tools/cli/ship/internal/commands/project_new.go
- tools/cli/ship/internal/commands/project_new_layout.go
- tools/cli/ship/internal/commands/project_new_test.go
- tools/cli/ship/internal/commands/module.go
- tools/cli/ship/internal/commands/module_test.go
- tools/cli/ship/internal/commands/testdata/project_new_layout.golden
- tools/cli/ship/internal/commands/testdata/project_new_layout_i18n.golden
- .docket/tickets/TKT-589.md

Remaining work
- Extend module env snippets for additional batteries as wiring becomes concrete.

AC status
- [x] ship new myapp generates a .env.example file in the project root
- [x] .env.example includes all required vars (APP_KEY, DATABASE_URL) with inline comments
- [x] .env.example includes optional vars with defaults (CACHE_DRIVER=memory, QUEUE_DRIVER=backlite)
- [x] ship module:add billing appends STRIPE_KEY, STRIPE_WEBHOOK_SECRET to .env.example
- [x] go test ./tools/cli/ship/... -count=1 -run New passes
