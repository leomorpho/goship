---
id: TKT-488
seq: 488
state: in-review
priority: 4
labels:
    - feature
    - test
    - roadmap:production-system
    - topo:leaf
    - topo:lane-goship
parent: TKT-484
blocked_by:
    - TKT-487
created_at: "2026-03-21T16:10:32Z"
updated_at: "2026-03-25T02:04:20Z"
started_at: "2026-03-25T02:00:33Z"
created_by: human:Leo Audibert
write_hash: 6b3972ef04be4651baa09c333936b2aafab0c45bad6717f795fb0c1c6daa9653
---

# TKT-488: Add tests proving upgraded apps still emit valid managed runtime contracts.

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-488` before committing. -->

## Description
Objective: Add tests proving upgraded apps still emit valid managed runtime contracts.

Context:
These tickets expose upgrade readiness into fleet-level automation. Keep the runtime report and upgrade report aligned so the control plane can trust the data without inference.

Rails/Laravel DX bar:
- Prefer one obvious convention, delete fallback paths, and keep the generated or framework-owned surface beautiful enough to feel like Rails or Laravel.

Likely files:
- tools/cli/ship/internal/commands/project_upgrade.go
- tools/cli/ship/internal/commands/runtime_report.go
- tools/cli/ship/internal/commands/runtime_report_contract_test.go
- tools/cli/ship/internal/commands/project_upgrade_test.go

Likely symbols:
- UpgradeReadinessReport
- RunRuntimeReport
- runtimeReportHandshake

Tests to add or update:
- tools/cli/ship/internal/commands/runtime_report_contract_test.go
- tools/cli/ship/internal/commands/project_upgrade_test.go

Verify commands:
- docket validate TKT-488
- go test ./tools/cli/ship/internal/commands -run "Upgrade|RuntimeReport" -count=1

Out of scope:
- Broad refactors outside the scoped ownership surface for this ticket.
- Writing roadmap docs that are not required to keep code and tests aligned.
- Taking shortcuts that preserve multiple competing conventions instead of choosing one clear path.

## Acceptance Criteria
- [ ] Code changes are limited to the described scope.
- [ ] Automated tests for the changed behavior pass.
- [ ] Verification commands and outcomes are recorded in ticket notes or handoff.

## Comments

### 2026-03-25T02:04:18Z — human:Leo Audibert
Added contract-focused tests proving upgraded apps continue to emit valid managed runtime report contracts.

What shipped:
- Added `TestRunRuntimeReport_UpgradedAppsRetainManagedContractShape_RedSpec`.
- The test upgrades fixture apps (`legacy` and `canonical` goose pins) via `ship upgrade apply --to v3.27.0`, then runs `ship runtime:report --json`.
- Assertions lock managed runtime contract invariants post-upgrade:
  - `handshake.schema_version = runtime-handshake-v1`
  - managed `mode`, `authority`, `registry_version`, `schema_version`
  - managed key source/rollback-target shape for `adapters.cache`

Verification:
- `go test ./tools/cli/ship/internal/commands -run "Upgrade|RuntimeReport" -count=1`
- `make test`
- `DOCKET_HOME=/tmp/docket-home docket validate TKT-488`
- pre-commit (`lefthook`) passed during commit

Commit:
- d40b64ba (`test(commands): verify upgraded apps keep managed runtime contracts`)

