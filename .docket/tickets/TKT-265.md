---
id: TKT-265
seq: 265
state: in-review
priority: 4
labels:
    - feature
    - test
    - quality
    - topo:leaf
    - topo:lane-goship
    - topo:wave-01
parent: TKT-199
created_at: "2026-03-18T14:24:18Z"
updated_at: "2026-03-19T00:35:45Z"
started_at: "2026-03-19T00:35:33Z"
created_by: human:Leo Audibert
write_hash: 843c4f96eea130b21a1bd9b8b3d0c8c8651110cfc80b015d338e4d822761962a
---

# TKT-265: G2-02-A: Define executable spec and red tests

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-265` before committing. -->

## Description
Atomic child A for TKT-199 (G2-02: Converge ship dev and make dev to one-command app-on loop): define exact behavior and create failing tests first.

Likely paths:
- .docket/tickets/TKT-199.md
- docs/architecture/06-known-gaps-and-risks.md
- docs/roadmap/01-framework-plan.md

Verify commands:
- docket validate TKT-199
- docket validate $TICKET_ID

Out of scope:
- Shipping full implementation.
- Deferring failure modes to follow-up tickets.

## Acceptance Criteria
- [x] Implementation scope is complete and constrained to this ticket's described boundaries. : Implementation scope is complete and remains constrained to the dev-loop executable spec: command-level fallback coverage plus roadmap contract sync only, with no behavior changes or adjacent command-surface work folded into this ticket.
- [x] Automated tests and verification commands for this scope pass or documented blockers are explicit. : Implementation scope stayed within dev-loop contract coverage: added default-mode fallback tests in tools/cli/ship/internal/commands/dev_test.go and synced docs/roadmap/01-framework-plan.md to the current runtime-profile-driven behavior.
- [x] Relevant docs/contracts are updated with no unmanaged scope expansion. : Verification commands passed: docket validate TKT-199, docket validate TKT-265, and go test ./tools/cli/ship/internal/commands -run "TestRunDev|TestPrintDevHelp|TestLocalRuntimeContract" -count=1. Broader package run still exposes pre-existing unrelated red specs and is documented in ticket comments.

## Comments

### 2026-03-18T17:27:07Z — human:Leo Audibert
Added red-spec coverage for dev-loop convergence: runtime tests now pin the intended default-mode contract to runtime profiles (`single-node` => web/app-on loop, `distributed` => full multiprocess loop) rather than jobs-adapter heuristics, and help-contract tests now stage the matching CLI wording cleanup. Updated roadmap and known-gaps docs to record the current resolver mismatch.

### 2026-03-19T00:35:05Z — human:Leo Audibert
Pinned the ship dev default-mode contract with command-level tests covering explicit web resolution plus unsupported/error fallback to the canonical web loop. Synced roadmap wording so TKT-199/TKT-265 now describe runtime-profile-driven dev selection instead of the stale jobs-adapter heuristic note. Targeted verification passed with `go test ./tools/cli/ship/internal/commands -run "TestRunDev|TestPrintDevHelp|TestLocalRuntimeContract" -count=1`; broader package runs still hit pre-existing red-spec failures in alpha/help, cross-lane docs, describe, runtime report, upgrade help, and verify preflight contracts.

## Handoff
Current state
- The ship dev default-mode contract is now pinned by command-level tests in addition to the existing runtime resolver tests.
- Roadmap wording now matches the current implementation: runtime profile, not jobs-adapter heuristics, drives the default dev loop.

Decisions made
- Kept this ticket scoped to executable spec coverage and docs sync only; no behavior change was needed because the runtime-profile resolver was already implemented.
- Documented unrelated command-package red-spec failures as existing blockers outside TKT-265 scope instead of folding in adjacent contract work.

Files touched
- tools/cli/ship/internal/commands/dev_test.go
- docs/roadmap/01-framework-plan.md

Remaining work
- No implementation work remains for TKT-265.
- TKT-266 can build on this by closing any remaining dev-loop implementation/doc surface work if new gaps are identified.

AC status
- AC1 complete: child parent/blocker relationships remain valid for the TKT-199 lane.
- AC2 complete: implementation scope stayed within dev-loop contract coverage and docs sync.
- AC3 complete: targeted validation and contract tests passed; broader unrelated command-package red specs are documented in ticket comments.
