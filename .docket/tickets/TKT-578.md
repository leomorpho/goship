---
id: TKT-578
seq: 578
state: done
priority: 3
labels:
    - feature
    - topo:leaf
    - topo:lane-goship
parent: TKT-189
created_at: "2026-03-24T02:35:53Z"
updated_at: "2026-03-24T14:52:23Z"
started_at: "2026-03-24T14:51:33Z"
completed_at: "2026-03-24T14:52:23Z"
created_by: human:Leo Audibert
write_hash: d71a0b7c89c0afacf1c7a9cb2f8056d9d270681ad17d375da21fd2fc57d9c132
---

# TKT-578: Add Otter in-memory cache adapter and wire it as the default dev/single-binary cache backend

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-578` before committing. -->

## Description
Add an Otter in-memory cache adapter so GoShip can run without Redis in standalone/single-binary mode. Wire it as the default dev-mode and single-binary cache backend. Redis remains available for production.

Likely paths:
- framework/core/interfaces.go
- container.go
- config/config.go

Verify commands:
- go test ./... -count=1 -run Cache
- docket validate $TICKET_ID

Out of scope:
- Changes to the Redis cache adapter or production multi-process path.
- Broad framework refactors outside the cache adapter surface.

## Acceptance Criteria
- [x] ship dev starts and performs cache get/set/delete without Redis running
- [x] Otter adapter implements the Cache interface: Get, Set, Delete, Flush
- [x] Config CACHE_DRIVER=memory selects Otter; CACHE_DRIVER=redis selects Redis adapter
- [x] go test ./... -count=1 -run Cache passes including in-memory adapter tests

## Handoff
Current state
- Otter cache adapter is implemented in `framework/repos/cache/otter.go` and wired through cache client selection in `framework/repos/cache/cache.go`.
- Single-binary defaults include cache adapter `otter` in config/runtime metadata.

Decisions made
- Preserve Redis cache path for production while using Otter defaults in single-binary/dev mode.
- Close ticket with existing implementation and reproducible cache-focused verification evidence.

Files touched
- .docket/tickets/TKT-578.md

Remaining work
- Further cache ergonomics/monitoring can land in separate follow-up tickets.

AC status
- [x] `go test ./... -count=1 -run Cache` passed locally, including `framework/repos/cache` adapter contract coverage.
- [x] Adapter resolution contract includes memory alias -> otter and explicit redis selection.
