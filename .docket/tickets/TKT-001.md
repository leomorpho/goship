---
id: TKT-001
seq: 1
state: done
priority: 2
created_at: "2026-03-11T02:08:26Z"
updated_at: "2026-03-11T02:08:26Z"
created_by: human:Leo Audibert
write_hash: d5047b74c261ff352980b1be7bdbd83e4ab7a24c92d9740bf3211a430bb11a79
---

# TKT-001: M03 A01 - Fix container initialization for optional services

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-001` before committing. -->

## Description
**Source:** `docs/roadmap/03-atomic-tasks.md`
**Roadmap Group:** Group A — Critical Bug Fixes
**Original Task Code:** `M03 A01`
**Original Status:** `[x] done`
**Original Dependencies:** nothing

**Status:** `[x] done`
**Depends on:** nothing
**Files:** `app/foundation/container.go`

**Context:** `initCache()`, `initNotifier()`, `initTasks()` are commented out in `NewContainer()`. Shutdown code calls `.Close()` on potentially nil fields — latent nil-pointer panics. See `docs/architecture/06-known-gaps-and-risks.md`.

**What to do:**
1. Read `app/foundation/container.go` fully.
2. Wrap each commented-out init in a config guard (e.g., `if c.Config.Cache.Enabled { c.initCache() }`). Add `Enabled` booleans to the config struct if they don't exist.
3. Audit all `.Close()` / shutdown calls — nil-check every optional service before calling.
4. Run `go build ./...` and `make test`.

**Done when:** No commented-out init calls remain. Shutdown is nil-safe. `go build` passes.

---

## Acceptance Criteria
- [x] No commented-out init calls remain. Shutdown is nil-safe. `go build` passes. — evidence: Imported from roadmap task marked done

