---
id: TKT-174
seq: 174
state: backlog
priority: 6
labels:
    - refactor
parent: TKT-170
blocked_by:
    - TKT-173
created_at: "2026-03-17T22:35:36Z"
updated_at: "2026-03-17T23:46:38Z"
created_by: human:Leo Audibert
write_hash: 8567fd18baf07bd3830319c0ac3b9521af34a3f43982386637e36250d316b7c6
---

# TKT-174: Migrate live DTOs out of app/contracts and delete unused contract types

## Description
Move actively used request DTOs from `app/contracts` into owning modules/controllers and delete unused legacy DTOs.

Context:
Only a subset of `app/contracts` types are active; many are stale artifacts.

Scope:
- Migrate live bind structs to ownership locations.
- Remove dead DTO files/types with no call sites.

Out of scope:
- Deleting the `app/contracts` directory itself (handled separately).

Likely paths:
- `app/web/controllers/*.go`
- `modules/**/routes*.go`
- `modules/**/contracts*.go` (new/updated)

Verify commands:
- `go test ./app/web/controllers/...`
- `go test ./modules/...`
- `go test ./...`

## Acceptance Criteria
- [x] All active request DTOs live with owning module/controller packages. : Moved live DTOs into owning controllers: contact.go, email_subscribe.go, managed_hooks.go, preferences.go; removed controller imports of app/contracts.
- [x] Unused legacy DTOs are removed with zero call sites remaining. : Deleted all legacy Go DTO files under app/contracts/*.go; no remaining app/contracts imports/callsites in app/modules/framework/cmd.
- [x] Touched route handlers continue to bind and validate correctly. : Validation path checks passed: go test ./app/web/controllers/... ; go test ./modules/... ; go test ./...

## Comments

### 2026-03-17T23:46:38Z — human:Leo Audibert
Implemented in commit b9084a44ee1473cda69e0d0d01f2b2323a107694 (refactor(contracts): migrate request DTOs into owning controllers). Tests: go test ./tools/cli/ship/internal/policies -run TestRunDoctorChecks_ContractUsage -count=1 ; go test ./app/web/controllers/... ; go test ./modules/... ; go test ./... ; pre-commit suite.

