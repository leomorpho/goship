---
id: TKT-499
seq: 499
state: in-review
priority: 4
labels:
    - feature
    - test
    - roadmap:production-system
    - topo:leaf
    - topo:lane-goship
parent: TKT-497
blocked_by:
    - TKT-498
created_at: "2026-03-21T16:10:38Z"
updated_at: "2026-03-25T02:15:27Z"
started_at: "2026-03-25T02:11:19Z"
created_by: human:Leo Audibert
write_hash: 86184411208ada489f1373b29918edd15274389c446a29f729cd7ec60502b99c
---

# TKT-499: Add module boundary tests that fail when modules depend on non-canonical framework internals.

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-499` before committing. -->

## Description
Objective: Add module boundary tests that fail when modules depend on non-canonical framework internals.

Context:
These tickets harden generators and modules against collisions and drift. Prefer canonical ownership and deterministic output over convenience patches.

Rails/Laravel DX bar:
- Prefer one obvious convention, delete fallback paths, and keep the generated or framework-owned surface beautiful enough to feel like Rails or Laravel.

Likely files:
- tools/cli/ship/internal/generators/
- modules/
- tools/cli/ship/internal/commands/module.go

Likely symbols:
- RunMakeModule
- checkModuleCompatibilityPolicy

Tests to add or update:
- tools/cli/ship/internal/generators/*_test.go
- modules/*/module_test.go
- modules/*/routes*_test.go

Verify commands:
- docket validate TKT-499
- go test ./tools/cli/ship/internal/generators ./modules/... -count=1

Out of scope:
- Broad refactors outside the scoped ownership surface for this ticket.
- Writing roadmap docs that are not required to keep code and tests aligned.
- Taking shortcuts that preserve multiple competing conventions instead of choosing one clear path.

## Acceptance Criteria
- [ ] Code changes are limited to the described scope.
- [ ] Automated tests for the changed behavior pass.
- [ ] Verification commands and outcomes are recorded in ticket notes or handoff.

## Comments

### 2026-03-25T02:15:25Z — human:Leo Audibert
Added module-boundary contract coverage for non-canonical framework internals.

What shipped:
- Added `TestModuleIsolationContract_FailsOnNonCanonicalFrameworkInternalDependency_RedSpec` in `tools/cli/ship/internal/commands/module_isolation_contract_test.go`.
- The new test creates a local module that imports `github.com/leomorpho/goship/framework/web/controllers` and verifies the module-isolation script fails with module/file/import diagnostics.
- This locks the boundary contract so module consumers cannot couple to non-canonical framework internals without a failing test signal.

Verification:
- `go test ./tools/cli/ship/internal/commands -run ModuleIsolationContract -count=1`
- `go test ./tools/cli/ship/internal/generators ./modules/... -count=1`
- `DOCKET_HOME=/tmp/docket-home docket validate TKT-499`
- pre-commit (`lefthook`) passed during commit

Commit:
- 3adf3c85 (`test(commands): enforce non-canonical module internal boundaries`)

