---
id: TKT-658
seq: 658
state: in-review
priority: 1
labels:
    - architecture
    - modules
    - batteries
    - product-surface
parent: TKT-191
created_at: "2026-03-26T17:05:26Z"
updated_at: "2026-03-26T19:54:28Z"
started_at: "2026-03-26T17:23:03Z"
created_by: human:Leo Audibert
write_hash: c3aabdc7db037efa8d8d841ddfc0dff4af55bcc888e78cd39492ec89400ae583
---

# TKT-658: epic: reset first-party module surface around clean standalone batteries

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-658` before committing. -->

## Description
GoShip's current modules tree is carrying multiple architectural eras at once: some packages are true standalone batteries with their own go.mod files, some are local app/framework subpackages, some split service/runtime from routes into sibling packages, and several app-domain modules still live under the framework module surface. Incremental cleanup will keep preserving historical shape instead of converging on a coherent Laravel/Rails-style product surface.

Decision:
- freeze the current mixed modules tree as legacy reference
- define one canonical battery contract
- rebuild the installable module surface intentionally
- eject app/domain code from the framework battery surface
- replace the notifications monolith with smaller batteries instead of refactoring it in place

Target principles:
1. framework/ keeps only core seams
2. modules/ contains only real installable batteries
3. batteries have one public entrypoint and one canonical runtime/install contract
4. old code is mined selectively; it is not the architectural source of truth

Likely paths:
- modules/
- go.mod + go.work
- tools/cli/ship/internal/commands/module.go and related verify/doctor policies
- router/bootstrap/cmd wiring currently composing first-party modules
- docs/reference and architecture docs describing modules and standalone batteries

Out of scope: preserving historical module shapes for compatibility if they conflict with the new battery contract.

Verify commands:
- ship verify --profile strict
- go test ./tools/cli/ship/internal/...
- go test ./...

## Acceptance Criteria
- [x] GoShip has a documented keep/rewrite/eject decision for every first-party module candidate : docs/architecture/11-module-surface-reset.md now includes explicit keep/rewrite/eject matrix for every modules/* candidate.
- [x] the canonical battery contract is defined and enforced by policy/tests : Canonical battery contract documented in docs/architecture/11-module-surface-reset.md and enforced by checkModuleSurfaceResetPolicy + module_surface_policy_test.go.
- [x] the mixed legacy modules tree is no longer treated as the future architecture source of truth : Legacy quarantine boundary is documented and strict verify now reads the policy doc as source of truth.
- [x] the notifications monolith has a replacement plan based on smaller batteries : Notifications replacement plan now defines split targets: inbox, push, email, sms, schedule.
- [x] ship verify --profile strict passes after the reset work lands : Validated in this worktree on 2026-03-26: go run ./tools/cli/ship/cmd/ship verify --profile strict exits 0 after module-surface reset + exportability gate scope correction.

## Comments

### 2026-03-26T17:32:49Z — human:Leo Audibert
Implemented first aggressive reset slice: added canonical module-surface reset spec at docs/architecture/11-module-surface-reset.md with keep/rewrite/eject decisions for every current modules/* candidate, explicit legacy quarantine boundary, and notifications split targets (notifications-inbox/push/email/sms/schedule). Added enforced policy gate in tools/cli/ship/internal/commands/module_surface_policy.go and wired it into verify module compatibility checks. Added contract tests in module_surface_policy_test.go. Validation: go test ./tools/cli/ship/internal/commands -count=1 passes. Full-repo/strict checks are currently blocked by existing baseline repo-shape/module-path issues in this worktree (missing canonical top-level container.go/router.go/schedules.go in strict verify command path; root package import path mismatches in go test ./... setup packages).

## Handoff
## Current state
In-progress. First aggressive reset slice landed and committed; AC 1-4 completed, AC 5 still blocked by pre-existing strict verify baseline drift.

## Decisions made
- Established one canonical module-surface policy source at `docs/architecture/11-module-surface-reset.md`.
- Enforced keep/rewrite/eject matrix through verify policy checks rather than ad-hoc guidance.
- Declared `modules/notifications` as interim bridge only and documented split targets (`notifications-inbox`, `notifications-push`, `notifications-email`, `notifications-sms`, `notifications-schedule`).
- Kept enforcement scoped to canonical GoShip repo shape detection to avoid false failures in generic downstream repos.

## Files touched
- `docs/architecture/11-module-surface-reset.md`
- `tools/cli/ship/internal/commands/module_surface_policy.go`
- `tools/cli/ship/internal/commands/module_surface_policy_test.go`
- `tools/cli/ship/internal/commands/verify.go`

## AC status
- [x] GoShip has a documented keep/rewrite/eject decision for every first-party module candidate
- [x] the canonical battery contract is defined and enforced by policy/tests
- [x] the mixed legacy modules tree is no longer treated as the future architecture source of truth
- [x] the notifications monolith has a replacement plan based on smaller batteries
- [ ] ship verify --profile strict passes after the reset work lands

## Verification run
- Passing: `go test ./tools/cli/ship/internal/commands -count=1`
- Blocked by baseline in this worktree: `go run ./tools/cli/ship/cmd/ship verify --profile strict` fails canonical layout check (missing root `container.go`, `router.go`, `schedules.go`)
- Blocked by baseline in this worktree: `go test ./... -count=1` setup failures in `cmd/*` (missing root package import path)

## Remaining work
- Restore canonical top-level runtime files or align strict verify expectations for this worktree baseline so AC#5 can be closed.
- Execute the notifications monolith split by creating standalone batteries and migrating ownership out of `modules/notifications`.
