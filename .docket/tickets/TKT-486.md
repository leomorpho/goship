---
id: TKT-486
seq: 486
state: in-review
priority: 4
labels:
    - feature
    - test
    - roadmap:production-system
    - topo:leaf
    - topo:lane-goship
parent: TKT-484
blocked_by:
    - TKT-485
created_at: "2026-03-21T16:10:32Z"
updated_at: "2026-03-25T01:55:43Z"
started_at: "2026-03-25T01:49:43Z"
created_by: human:Leo Audibert
write_hash: 255df4e42e5ab959a57e3a58bc4e09ce38a12effceee5f76b683aa1643b779c4
---

# TKT-486: Make runtime reports expose current framework version and module adoption for fleet visibility.

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-486` before committing. -->

## Description
Objective: Make runtime reports expose current framework version and module adoption for fleet visibility.

Context:
These tickets expose upgrade readiness into fleet-level automation. Keep the runtime report and upgrade report aligned so the control plane can trust the data without inference.

Rails/Laravel DX bar:
- Prefer one obvious convention, delete fallback paths, and keep the generated or framework-owned surface beautiful enough to feel like Rails or Laravel.

Likely files:
- tools/cli/ship/internal/commands/project_upgrade.go
- tools/cli/ship/internal/commands/runtime_report.go
- tools/cli/ship/internal/commands/runtime_report_contract_test.go
- tools/cli/ship/internal/commands/project_upgrade_test.go

Likely symbols:
- UpgradeReadinessReport
- RunRuntimeReport
- runtimeReportHandshake

Tests to add or update:
- tools/cli/ship/internal/commands/runtime_report_contract_test.go
- tools/cli/ship/internal/commands/project_upgrade_test.go

Verify commands:
- docket validate TKT-486
- go test ./tools/cli/ship/internal/commands -run "Upgrade|RuntimeReport" -count=1

Out of scope:
- Broad refactors outside the scoped ownership surface for this ticket.
- Writing roadmap docs that are not required to keep code and tests aligned.
- Taking shortcuts that preserve multiple competing conventions instead of choosing one clear path.

## Acceptance Criteria
- [ ] Code changes are limited to the described scope.
- [ ] Automated tests for the changed behavior pass.
- [ ] Verification commands and outcomes are recorded in ticket notes or handoff.

## Comments

### 2026-03-25T01:55:41Z — human:Leo Audibert
Implemented framework-version visibility in runtime reports while keeping module adoption surfaced for fleet orchestration.

What shipped:
- Added `framework_version` to `ship runtime:report --json` payload.
- Runtime report now derives framework version from the canonical/legacy `gooseGoRunRef` marker in `tools/cli/ship/internal/cli/cli.go`.
- Added shared version-detection helpers in upgrade command code so runtime and upgrade paths use the same parsing convention.
- Extended runtime report tests/contracts to lock `framework_version` and module-adoption visibility.
- Updated CLI + architecture docs and doc-sync contract expectations to include current framework version in runtime report surfaces.

Verification:
- `go test ./tools/cli/ship/internal/commands -run "Upgrade|RuntimeReport" -count=1`
- `make test`
- `DOCKET_HOME=/tmp/docket-home docket validate TKT-486`
- pre-commit (`lefthook`) passed during commit

Commit:
- f8229ec0 (`feat(cli): expose framework version in runtime report`)

