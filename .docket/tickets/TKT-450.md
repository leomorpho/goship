---
id: TKT-450
seq: 450
state: in-review
priority: 4
labels:
    - feature
    - test
    - roadmap:production-system
    - topo:leaf
    - topo:lane-goship
parent: TKT-444
blocked_by:
    - TKT-449
created_at: "2026-03-21T16:10:16Z"
updated_at: "2026-03-24T18:16:40Z"
started_at: "2026-03-24T18:11:05Z"
created_by: human:Leo Audibert
write_hash: ba81d915a30ee7cb42e8bfea9f71ddef202fe1d21234a01e9078b7bb160cb986
---

# TKT-450: Make generator path ownership explicit and fail when output escapes canonical app-owned locations.

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-450` before committing. -->

## Description
Objective: Make generator path ownership explicit and fail when output escapes canonical app-owned locations.

Context:
This is the generator contract surface. Keep generator output deterministic, readable, and reversible, with one canonical file placement strategy rather than ad hoc per-generator behavior.

Rails/Laravel DX bar:
- Prefer one obvious convention, delete fallback paths, and keep the generated or framework-owned surface beautiful enough to feel like Rails or Laravel.

Likely files:
- tools/cli/ship/internal/generators/
- tools/cli/ship/internal/commands/module.go
- tools/cli/ship/internal/commands/make_island_test.go
- tools/cli/ship/internal/generators/snapshot_contract_test.go

Likely symbols:
- RunMakeModule
- RunMakeJob
- RunMakeResource
- RunMakeController

Tests to add or update:
- tools/cli/ship/internal/generators/*_test.go
- tools/cli/ship/internal/generators/*_integration_test.go
- tools/cli/ship/internal/generators/snapshot_contract_test.go

Verify commands:
- docket validate TKT-450
- go test ./tools/cli/ship/internal/generators ./tools/cli/ship/internal/commands -count=1

Out of scope:
- Broad refactors outside the scoped ownership surface for this ticket.
- Writing roadmap docs that are not required to keep code and tests aligned.
- Taking shortcuts that preserve multiple competing conventions instead of choosing one clear path.

## Acceptance Criteria
- [x] Code changes are limited to the described scope. : Changes are limited to generator path-validation surfaces plus CLI reference sync.
- [x] Automated tests for the changed behavior pass. : go test ./tools/cli/ship/internal/generators ./tools/cli/ship/internal/commands -count=1 and make test passed.
- [x] Verification commands and outcomes are recorded in ticket notes or handoff. : Handoff records verification commands and pass outcomes.

## Handoff
## Current state
Implemented and verified. Generator path ownership is now explicit and canonical for app/module generator roots.

## Decisions made
- Added shared path-normalization guard `normalizeOwnedGeneratorPath`.
- Enforced canonical `--path` ownership in parser flows:
  - `ship make:controller` requires `app`.
  - `ship make:resource` requires `app`.
  - `ship make:module` requires `modules`.
- Added TDD coverage for escape/divergence paths and updated module parse expectations.

## Files touched
- tools/cli/ship/internal/generators/util.go
- tools/cli/ship/internal/generators/controller.go
- tools/cli/ship/internal/generators/resource.go
- tools/cli/ship/internal/generators/module.go
- tools/cli/ship/internal/generators/controller_test.go
- tools/cli/ship/internal/generators/resource_test.go
- tools/cli/ship/internal/generators/module_integration_test.go
- docs/reference/01-cli.md

## Remaining work
None for TKT-450.

## AC status
- [x] Code changes are limited to the described scope.
- [x] Automated tests for the changed behavior pass.
- [x] Verification commands and outcomes are recorded in ticket notes or handoff.

## Verification
- `docket validate TKT-450` (pass)
- `go test ./tools/cli/ship/internal/generators ./tools/cli/ship/internal/commands -count=1` (pass)
- `make test` (pass)
