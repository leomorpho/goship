---
id: TKT-617
seq: 617
state: in-review
priority: 4
labels:
    - feature
    - test
    - roadmap:production-system
    - topo:leaf
    - topo:lane-goship
parent: TKT-608
blocked_by:
    - TKT-616
created_at: "2026-03-25T17:06:22Z"
updated_at: "2026-03-25T18:23:57Z"
started_at: "2026-03-25T18:19:25Z"
created_by: human:Leo Audibert
write_hash: dbbb6ce0dbe4a4b0336ce3acf33c0ddd8bd26447af5055e7ba2b10cf825c0e58
---

# TKT-617: Add runtime-report and stable access-classification tools to ship-mcp for stronger agent context.

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-617` before committing. -->

## Description
Objective: Add runtime-report and stable access-classification tools to ship-mcp for stronger agent context.

Context:
The MCP server exposes routes and modules, but it still lacks a direct runtime-report tool and its current route filtering cannot distinguish admin routes properly. Agents need one reliable structured surface for runtime facts and route access levels.

Rails/Laravel DX bar:
- Agent tooling should expose the same trustworthy facts a strong human operator would reach for first.
- Filters that silently do nothing are worse than no filter at all.

Likely paths:
- tools/mcp/ship/internal/server/tools.go
- tools/mcp/ship/internal/server/tools_ship.go
- tools/mcp/ship/internal/server/tools_test.go
- tools/cli/ship/internal/commands/describe.go
- tools/cli/ship/internal/commands/describe*_test.go
- tools/cli/ship/internal/commands/runtime_report.go

Likely symbols:
- `ship_runtime_report`
- route access classification (`public`, `auth`, `admin`)
- `ship describe` JSON payload

Tests to add or update:
- MCP tests for new runtime-report tool and route classification behavior.
- CLI describe contract tests if the JSON schema must grow to support stable access classification.

Verify commands:
- docket validate TKT-613
- go test ./tools/mcp/ship/... ./tools/cli/ship/internal/commands -count=1

Out of scope:
- General MCP redesign beyond these context surfaces.
- Framework runtime behavior unrelated to reporting and route inventory.
- Hosted-control-plane-only tooling.

## Acceptance Criteria
- [x] Code changes are limited to the described scope. : Changes scoped to MCP tools/server tests and CLI describe route schema/tests.
- [x] Automated tests cover the new MCP/runtime-report surface and stable route access classification behavior. : Added MCP tests for ship_runtime_report and stable public/auth/admin route filtering with access-classification behavior.
- [x] Verification commands and outcomes are recorded in ticket notes or handoff. : Verification command outcomes recorded in ticket comment.

## Comments

### 2026-03-25T18:22:29Z — human:Leo Audibert
Implemented MCP reporting/context improvements:
- Added `ship_runtime_report` MCP tool that runs `ship runtime:report --json`.
- Added stable route access classification (`public`, `auth`, `admin`) via new `access` field in `ship describe` route payload.
- Updated MCP `ship_routes` filtering to use access classification so `admin` no longer silently no-ops.
- Added/updated tests for runtime-report tool and admin/auth/public filtering behavior.

Verification:
- docket validate TKT-613
- go test ./tools/mcp/ship/... ./tools/cli/ship/internal/commands -count=1

### 2026-03-25T18:23:56Z — human:Leo Audibert
Committed as 739261d3 with Ticket trailer. Pre-commit suite and target verification tests passed.

## Handoff
Added ship_runtime_report MCP tool plus stable route access classification in describe payload (access=public|auth|admin). ship_routes filtering now uses access classification, including functional admin filtering. Updated tests and describe pretty golden accordingly. Verification: docket validate TKT-613; go test ./tools/mcp/ship/... ./tools/cli/ship/internal/commands -count=1. Commit: 739261d3.
