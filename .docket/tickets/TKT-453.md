---
id: TKT-453
seq: 453
state: in-review
priority: 4
labels:
    - feature
    - test
    - roadmap:production-system
    - topo:leaf
    - topo:lane-goship
parent: TKT-452
blocked_by:
    - TKT-451
created_at: "2026-03-21T16:10:17Z"
updated_at: "2026-03-24T19:11:02Z"
started_at: "2026-03-24T19:04:36Z"
created_by: human:Leo Audibert
write_hash: 94ba4a1eeed6edd4526b90178bdab271b66c8deacb2715537f7f894b551a1f30
---

# TKT-453: Define a battery install contract for routes, config, assets, jobs, templates, and migrations.

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-453` before committing. -->

## Description
Objective: Define a battery install contract for routes, config, assets, jobs, templates, and migrations.

Context:
These tickets make batteries reversible and composable. Treat install and remove as first-class framework behavior, not best-effort scripts.

Rails/Laravel DX bar:
- Prefer one obvious convention, delete fallback paths, and keep the generated or framework-owned surface beautiful enough to feel like Rails or Laravel.

Likely files:
- tools/cli/ship/internal/commands/module.go
- tools/cli/ship/internal/commands/module_test.go
- tools/cli/ship/internal/generators/module.go
- modules/*/module.go
- docs/reference/01-cli.md

Likely symbols:
- RunMakeModule
- ParseMakeModuleArgs
- renderModuleEntrypoint

Tests to add or update:
- tools/cli/ship/internal/commands/module_test.go
- tools/cli/ship/internal/generators/module_integration_test.go
- modules/*/module_test.go

Verify commands:
- docket validate TKT-453
- go test ./tools/cli/ship/internal/commands ./tools/cli/ship/internal/generators ./modules/... -count=1

Out of scope:
- Broad refactors outside the scoped ownership surface for this ticket.
- Writing roadmap docs that are not required to keep code and tests aligned.
- Taking shortcuts that preserve multiple competing conventions instead of choosing one clear path.

## Acceptance Criteria
- [x] Code changes are limited to the described scope. : Changes remain within module command/generator contract surfaces and CLI docs sync.
- [x] Automated tests for the changed behavior pass. : docket validate TKT-453, go test ./tools/cli/ship/internal/commands ./tools/cli/ship/internal/generators ./modules/... -count=1, and make test passed.
- [x] Verification commands and outcomes are recorded in ticket notes or handoff. : Handoff includes verification commands and outcomes.

## Handoff
## Current state
Implemented and verified. Battery install contract coverage is now explicit in module add output and scaffolded module entrypoints.

## Decisions made
- Added install-contract model on module catalog entries with grouped surfaces: `routes`, `config`, `assets`, `jobs`, `templates`, `migrations`.
- `ship module:add` now prints install-contract sections so install impact is reviewable in dry-run and normal flows.
- `make:module` scaffold now emits `InstallContract` + `Contract()` in generated `module.go`.
- Added tests for catalog coverage, dry-run install-contract output, and scaffold contract tokens.
- Synced CLI reference docs with both new behaviors.

## Files touched
- tools/cli/ship/internal/commands/module.go
- tools/cli/ship/internal/commands/module_test.go
- tools/cli/ship/internal/generators/module.go
- tools/cli/ship/internal/generators/module_integration_test.go
- docs/reference/01-cli.md

## Remaining work
None for TKT-453.

## AC status
- [x] Code changes are limited to the described scope.
- [x] Automated tests for the changed behavior pass.
- [x] Verification commands and outcomes are recorded in ticket notes or handoff.

## Verification
- `docket validate TKT-453` (pass)
- `go test ./tools/cli/ship/internal/commands ./tools/cli/ship/internal/generators ./modules/... -count=1` (pass)
- `make test` (pass)
