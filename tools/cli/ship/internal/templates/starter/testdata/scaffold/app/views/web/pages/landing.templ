package pages

// Renders: starter landing page.
templ Landing() {
	<section data-component="starter-landing" class="p-6">
		<h1>GoShip Starter</h1>
		<p>Landing page scaffold with auth entrypoints and no optional modules enabled by default.</p>
	</section>
}
