// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/mikestefanello/pagoda/ent/emailsubscription"
	"github.com/mikestefanello/pagoda/ent/emailsubscriptiontype"
	"github.com/mikestefanello/pagoda/ent/predicate"
)

// EmailSubscriptionTypeQuery is the builder for querying EmailSubscriptionType entities.
type EmailSubscriptionTypeQuery struct {
	config
	ctx            *QueryContext
	order          []emailsubscriptiontype.OrderOption
	inters         []Interceptor
	predicates     []predicate.EmailSubscriptionType
	withSubscriber *EmailSubscriptionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EmailSubscriptionTypeQuery builder.
func (estq *EmailSubscriptionTypeQuery) Where(ps ...predicate.EmailSubscriptionType) *EmailSubscriptionTypeQuery {
	estq.predicates = append(estq.predicates, ps...)
	return estq
}

// Limit the number of records to be returned by this query.
func (estq *EmailSubscriptionTypeQuery) Limit(limit int) *EmailSubscriptionTypeQuery {
	estq.ctx.Limit = &limit
	return estq
}

// Offset to start from.
func (estq *EmailSubscriptionTypeQuery) Offset(offset int) *EmailSubscriptionTypeQuery {
	estq.ctx.Offset = &offset
	return estq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (estq *EmailSubscriptionTypeQuery) Unique(unique bool) *EmailSubscriptionTypeQuery {
	estq.ctx.Unique = &unique
	return estq
}

// Order specifies how the records should be ordered.
func (estq *EmailSubscriptionTypeQuery) Order(o ...emailsubscriptiontype.OrderOption) *EmailSubscriptionTypeQuery {
	estq.order = append(estq.order, o...)
	return estq
}

// QuerySubscriber chains the current query on the "subscriber" edge.
func (estq *EmailSubscriptionTypeQuery) QuerySubscriber() *EmailSubscriptionQuery {
	query := (&EmailSubscriptionClient{config: estq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := estq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := estq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(emailsubscriptiontype.Table, emailsubscriptiontype.FieldID, selector),
			sqlgraph.To(emailsubscription.Table, emailsubscription.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, emailsubscriptiontype.SubscriberTable, emailsubscriptiontype.SubscriberPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(estq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first EmailSubscriptionType entity from the query.
// Returns a *NotFoundError when no EmailSubscriptionType was found.
func (estq *EmailSubscriptionTypeQuery) First(ctx context.Context) (*EmailSubscriptionType, error) {
	nodes, err := estq.Limit(1).All(setContextOp(ctx, estq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{emailsubscriptiontype.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (estq *EmailSubscriptionTypeQuery) FirstX(ctx context.Context) *EmailSubscriptionType {
	node, err := estq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EmailSubscriptionType ID from the query.
// Returns a *NotFoundError when no EmailSubscriptionType ID was found.
func (estq *EmailSubscriptionTypeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = estq.Limit(1).IDs(setContextOp(ctx, estq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{emailsubscriptiontype.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (estq *EmailSubscriptionTypeQuery) FirstIDX(ctx context.Context) int {
	id, err := estq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EmailSubscriptionType entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EmailSubscriptionType entity is found.
// Returns a *NotFoundError when no EmailSubscriptionType entities are found.
func (estq *EmailSubscriptionTypeQuery) Only(ctx context.Context) (*EmailSubscriptionType, error) {
	nodes, err := estq.Limit(2).All(setContextOp(ctx, estq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{emailsubscriptiontype.Label}
	default:
		return nil, &NotSingularError{emailsubscriptiontype.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (estq *EmailSubscriptionTypeQuery) OnlyX(ctx context.Context) *EmailSubscriptionType {
	node, err := estq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EmailSubscriptionType ID in the query.
// Returns a *NotSingularError when more than one EmailSubscriptionType ID is found.
// Returns a *NotFoundError when no entities are found.
func (estq *EmailSubscriptionTypeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = estq.Limit(2).IDs(setContextOp(ctx, estq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{emailsubscriptiontype.Label}
	default:
		err = &NotSingularError{emailsubscriptiontype.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (estq *EmailSubscriptionTypeQuery) OnlyIDX(ctx context.Context) int {
	id, err := estq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EmailSubscriptionTypes.
func (estq *EmailSubscriptionTypeQuery) All(ctx context.Context) ([]*EmailSubscriptionType, error) {
	ctx = setContextOp(ctx, estq.ctx, ent.OpQueryAll)
	if err := estq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EmailSubscriptionType, *EmailSubscriptionTypeQuery]()
	return withInterceptors[[]*EmailSubscriptionType](ctx, estq, qr, estq.inters)
}

// AllX is like All, but panics if an error occurs.
func (estq *EmailSubscriptionTypeQuery) AllX(ctx context.Context) []*EmailSubscriptionType {
	nodes, err := estq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EmailSubscriptionType IDs.
func (estq *EmailSubscriptionTypeQuery) IDs(ctx context.Context) (ids []int, err error) {
	if estq.ctx.Unique == nil && estq.path != nil {
		estq.Unique(true)
	}
	ctx = setContextOp(ctx, estq.ctx, ent.OpQueryIDs)
	if err = estq.Select(emailsubscriptiontype.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (estq *EmailSubscriptionTypeQuery) IDsX(ctx context.Context) []int {
	ids, err := estq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (estq *EmailSubscriptionTypeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, estq.ctx, ent.OpQueryCount)
	if err := estq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, estq, querierCount[*EmailSubscriptionTypeQuery](), estq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (estq *EmailSubscriptionTypeQuery) CountX(ctx context.Context) int {
	count, err := estq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (estq *EmailSubscriptionTypeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, estq.ctx, ent.OpQueryExist)
	switch _, err := estq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (estq *EmailSubscriptionTypeQuery) ExistX(ctx context.Context) bool {
	exist, err := estq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EmailSubscriptionTypeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (estq *EmailSubscriptionTypeQuery) Clone() *EmailSubscriptionTypeQuery {
	if estq == nil {
		return nil
	}
	return &EmailSubscriptionTypeQuery{
		config:         estq.config,
		ctx:            estq.ctx.Clone(),
		order:          append([]emailsubscriptiontype.OrderOption{}, estq.order...),
		inters:         append([]Interceptor{}, estq.inters...),
		predicates:     append([]predicate.EmailSubscriptionType{}, estq.predicates...),
		withSubscriber: estq.withSubscriber.Clone(),
		// clone intermediate query.
		sql:  estq.sql.Clone(),
		path: estq.path,
	}
}

// WithSubscriber tells the query-builder to eager-load the nodes that are connected to
// the "subscriber" edge. The optional arguments are used to configure the query builder of the edge.
func (estq *EmailSubscriptionTypeQuery) WithSubscriber(opts ...func(*EmailSubscriptionQuery)) *EmailSubscriptionTypeQuery {
	query := (&EmailSubscriptionClient{config: estq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	estq.withSubscriber = query
	return estq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EmailSubscriptionType.Query().
//		GroupBy(emailsubscriptiontype.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (estq *EmailSubscriptionTypeQuery) GroupBy(field string, fields ...string) *EmailSubscriptionTypeGroupBy {
	estq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EmailSubscriptionTypeGroupBy{build: estq}
	grbuild.flds = &estq.ctx.Fields
	grbuild.label = emailsubscriptiontype.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.EmailSubscriptionType.Query().
//		Select(emailsubscriptiontype.FieldCreatedAt).
//		Scan(ctx, &v)
func (estq *EmailSubscriptionTypeQuery) Select(fields ...string) *EmailSubscriptionTypeSelect {
	estq.ctx.Fields = append(estq.ctx.Fields, fields...)
	sbuild := &EmailSubscriptionTypeSelect{EmailSubscriptionTypeQuery: estq}
	sbuild.label = emailsubscriptiontype.Label
	sbuild.flds, sbuild.scan = &estq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EmailSubscriptionTypeSelect configured with the given aggregations.
func (estq *EmailSubscriptionTypeQuery) Aggregate(fns ...AggregateFunc) *EmailSubscriptionTypeSelect {
	return estq.Select().Aggregate(fns...)
}

func (estq *EmailSubscriptionTypeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range estq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, estq); err != nil {
				return err
			}
		}
	}
	for _, f := range estq.ctx.Fields {
		if !emailsubscriptiontype.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if estq.path != nil {
		prev, err := estq.path(ctx)
		if err != nil {
			return err
		}
		estq.sql = prev
	}
	return nil
}

func (estq *EmailSubscriptionTypeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EmailSubscriptionType, error) {
	var (
		nodes       = []*EmailSubscriptionType{}
		_spec       = estq.querySpec()
		loadedTypes = [1]bool{
			estq.withSubscriber != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EmailSubscriptionType).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EmailSubscriptionType{config: estq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, estq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := estq.withSubscriber; query != nil {
		if err := estq.loadSubscriber(ctx, query, nodes,
			func(n *EmailSubscriptionType) { n.Edges.Subscriber = []*EmailSubscription{} },
			func(n *EmailSubscriptionType, e *EmailSubscription) {
				n.Edges.Subscriber = append(n.Edges.Subscriber, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (estq *EmailSubscriptionTypeQuery) loadSubscriber(ctx context.Context, query *EmailSubscriptionQuery, nodes []*EmailSubscriptionType, init func(*EmailSubscriptionType), assign func(*EmailSubscriptionType, *EmailSubscription)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int]*EmailSubscriptionType)
	nids := make(map[int]map[*EmailSubscriptionType]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(emailsubscriptiontype.SubscriberTable)
		s.Join(joinT).On(s.C(emailsubscription.FieldID), joinT.C(emailsubscriptiontype.SubscriberPrimaryKey[0]))
		s.Where(sql.InValues(joinT.C(emailsubscriptiontype.SubscriberPrimaryKey[1]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(emailsubscriptiontype.SubscriberPrimaryKey[1]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := int(values[0].(*sql.NullInt64).Int64)
				inValue := int(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*EmailSubscriptionType]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*EmailSubscription](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "subscriber" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}

func (estq *EmailSubscriptionTypeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := estq.querySpec()
	_spec.Node.Columns = estq.ctx.Fields
	if len(estq.ctx.Fields) > 0 {
		_spec.Unique = estq.ctx.Unique != nil && *estq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, estq.driver, _spec)
}

func (estq *EmailSubscriptionTypeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(emailsubscriptiontype.Table, emailsubscriptiontype.Columns, sqlgraph.NewFieldSpec(emailsubscriptiontype.FieldID, field.TypeInt))
	_spec.From = estq.sql
	if unique := estq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if estq.path != nil {
		_spec.Unique = true
	}
	if fields := estq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, emailsubscriptiontype.FieldID)
		for i := range fields {
			if fields[i] != emailsubscriptiontype.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := estq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := estq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := estq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := estq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (estq *EmailSubscriptionTypeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(estq.driver.Dialect())
	t1 := builder.Table(emailsubscriptiontype.Table)
	columns := estq.ctx.Fields
	if len(columns) == 0 {
		columns = emailsubscriptiontype.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if estq.sql != nil {
		selector = estq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if estq.ctx.Unique != nil && *estq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range estq.predicates {
		p(selector)
	}
	for _, p := range estq.order {
		p(selector)
	}
	if offset := estq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := estq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EmailSubscriptionTypeGroupBy is the group-by builder for EmailSubscriptionType entities.
type EmailSubscriptionTypeGroupBy struct {
	selector
	build *EmailSubscriptionTypeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (estgb *EmailSubscriptionTypeGroupBy) Aggregate(fns ...AggregateFunc) *EmailSubscriptionTypeGroupBy {
	estgb.fns = append(estgb.fns, fns...)
	return estgb
}

// Scan applies the selector query and scans the result into the given value.
func (estgb *EmailSubscriptionTypeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, estgb.build.ctx, ent.OpQueryGroupBy)
	if err := estgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EmailSubscriptionTypeQuery, *EmailSubscriptionTypeGroupBy](ctx, estgb.build, estgb, estgb.build.inters, v)
}

func (estgb *EmailSubscriptionTypeGroupBy) sqlScan(ctx context.Context, root *EmailSubscriptionTypeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(estgb.fns))
	for _, fn := range estgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*estgb.flds)+len(estgb.fns))
		for _, f := range *estgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*estgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := estgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EmailSubscriptionTypeSelect is the builder for selecting fields of EmailSubscriptionType entities.
type EmailSubscriptionTypeSelect struct {
	*EmailSubscriptionTypeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ests *EmailSubscriptionTypeSelect) Aggregate(fns ...AggregateFunc) *EmailSubscriptionTypeSelect {
	ests.fns = append(ests.fns, fns...)
	return ests
}

// Scan applies the selector query and scans the result into the given value.
func (ests *EmailSubscriptionTypeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ests.ctx, ent.OpQuerySelect)
	if err := ests.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EmailSubscriptionTypeQuery, *EmailSubscriptionTypeSelect](ctx, ests.EmailSubscriptionTypeQuery, ests, ests.inters, v)
}

func (ests *EmailSubscriptionTypeSelect) sqlScan(ctx context.Context, root *EmailSubscriptionTypeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ests.fns))
	for _, fn := range ests.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ests.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ests.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
