// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/mikestefanello/pagoda/ent/phoneverificationcode"
	"github.com/mikestefanello/pagoda/ent/profile"
)

// PhoneVerificationCode is the model entity for the PhoneVerificationCode schema.
type PhoneVerificationCode struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// The verification code
	Code string `json:"code,omitempty"`
	// ProfileID holds the value of the "profile_id" field.
	ProfileID int `json:"profile_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PhoneVerificationCodeQuery when eager-loading is set.
	Edges        PhoneVerificationCodeEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PhoneVerificationCodeEdges holds the relations/edges for other nodes in the graph.
type PhoneVerificationCodeEdges struct {
	// Profile holds the value of the profile edge.
	Profile *Profile `json:"profile,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ProfileOrErr returns the Profile value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PhoneVerificationCodeEdges) ProfileOrErr() (*Profile, error) {
	if e.Profile != nil {
		return e.Profile, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: profile.Label}
	}
	return nil, &NotLoadedError{edge: "profile"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PhoneVerificationCode) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case phoneverificationcode.FieldID, phoneverificationcode.FieldProfileID:
			values[i] = new(sql.NullInt64)
		case phoneverificationcode.FieldCode:
			values[i] = new(sql.NullString)
		case phoneverificationcode.FieldCreatedAt, phoneverificationcode.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PhoneVerificationCode fields.
func (pvc *PhoneVerificationCode) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case phoneverificationcode.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			pvc.ID = int(value.Int64)
		case phoneverificationcode.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pvc.CreatedAt = value.Time
			}
		case phoneverificationcode.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				pvc.UpdatedAt = value.Time
			}
		case phoneverificationcode.FieldCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field code", values[i])
			} else if value.Valid {
				pvc.Code = value.String
			}
		case phoneverificationcode.FieldProfileID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field profile_id", values[i])
			} else if value.Valid {
				pvc.ProfileID = int(value.Int64)
			}
		default:
			pvc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PhoneVerificationCode.
// This includes values selected through modifiers, order, etc.
func (pvc *PhoneVerificationCode) Value(name string) (ent.Value, error) {
	return pvc.selectValues.Get(name)
}

// QueryProfile queries the "profile" edge of the PhoneVerificationCode entity.
func (pvc *PhoneVerificationCode) QueryProfile() *ProfileQuery {
	return NewPhoneVerificationCodeClient(pvc.config).QueryProfile(pvc)
}

// Update returns a builder for updating this PhoneVerificationCode.
// Note that you need to call PhoneVerificationCode.Unwrap() before calling this method if this PhoneVerificationCode
// was returned from a transaction, and the transaction was committed or rolled back.
func (pvc *PhoneVerificationCode) Update() *PhoneVerificationCodeUpdateOne {
	return NewPhoneVerificationCodeClient(pvc.config).UpdateOne(pvc)
}

// Unwrap unwraps the PhoneVerificationCode entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pvc *PhoneVerificationCode) Unwrap() *PhoneVerificationCode {
	_tx, ok := pvc.config.driver.(*txDriver)
	if !ok {
		panic("ent: PhoneVerificationCode is not a transactional entity")
	}
	pvc.config.driver = _tx.drv
	return pvc
}

// String implements the fmt.Stringer.
func (pvc *PhoneVerificationCode) String() string {
	var builder strings.Builder
	builder.WriteString("PhoneVerificationCode(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pvc.ID))
	builder.WriteString("created_at=")
	builder.WriteString(pvc.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(pvc.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("code=")
	builder.WriteString(pvc.Code)
	builder.WriteString(", ")
	builder.WriteString("profile_id=")
	builder.WriteString(fmt.Sprintf("%v", pvc.ProfileID))
	builder.WriteByte(')')
	return builder.String()
}

// PhoneVerificationCodes is a parsable slice of PhoneVerificationCode.
type PhoneVerificationCodes []*PhoneVerificationCode
