// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/mikestefanello/pagoda/ent/lastseenonline"
	"github.com/mikestefanello/pagoda/ent/predicate"
	"github.com/mikestefanello/pagoda/ent/user"
)

// LastSeenOnlineQuery is the builder for querying LastSeenOnline entities.
type LastSeenOnlineQuery struct {
	config
	ctx        *QueryContext
	order      []lastseenonline.OrderOption
	inters     []Interceptor
	predicates []predicate.LastSeenOnline
	withUser   *UserQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LastSeenOnlineQuery builder.
func (lsoq *LastSeenOnlineQuery) Where(ps ...predicate.LastSeenOnline) *LastSeenOnlineQuery {
	lsoq.predicates = append(lsoq.predicates, ps...)
	return lsoq
}

// Limit the number of records to be returned by this query.
func (lsoq *LastSeenOnlineQuery) Limit(limit int) *LastSeenOnlineQuery {
	lsoq.ctx.Limit = &limit
	return lsoq
}

// Offset to start from.
func (lsoq *LastSeenOnlineQuery) Offset(offset int) *LastSeenOnlineQuery {
	lsoq.ctx.Offset = &offset
	return lsoq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (lsoq *LastSeenOnlineQuery) Unique(unique bool) *LastSeenOnlineQuery {
	lsoq.ctx.Unique = &unique
	return lsoq
}

// Order specifies how the records should be ordered.
func (lsoq *LastSeenOnlineQuery) Order(o ...lastseenonline.OrderOption) *LastSeenOnlineQuery {
	lsoq.order = append(lsoq.order, o...)
	return lsoq
}

// QueryUser chains the current query on the "user" edge.
func (lsoq *LastSeenOnlineQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: lsoq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := lsoq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := lsoq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lastseenonline.Table, lastseenonline.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, lastseenonline.UserTable, lastseenonline.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(lsoq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LastSeenOnline entity from the query.
// Returns a *NotFoundError when no LastSeenOnline was found.
func (lsoq *LastSeenOnlineQuery) First(ctx context.Context) (*LastSeenOnline, error) {
	nodes, err := lsoq.Limit(1).All(setContextOp(ctx, lsoq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{lastseenonline.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (lsoq *LastSeenOnlineQuery) FirstX(ctx context.Context) *LastSeenOnline {
	node, err := lsoq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LastSeenOnline ID from the query.
// Returns a *NotFoundError when no LastSeenOnline ID was found.
func (lsoq *LastSeenOnlineQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = lsoq.Limit(1).IDs(setContextOp(ctx, lsoq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{lastseenonline.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (lsoq *LastSeenOnlineQuery) FirstIDX(ctx context.Context) int {
	id, err := lsoq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LastSeenOnline entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LastSeenOnline entity is found.
// Returns a *NotFoundError when no LastSeenOnline entities are found.
func (lsoq *LastSeenOnlineQuery) Only(ctx context.Context) (*LastSeenOnline, error) {
	nodes, err := lsoq.Limit(2).All(setContextOp(ctx, lsoq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{lastseenonline.Label}
	default:
		return nil, &NotSingularError{lastseenonline.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (lsoq *LastSeenOnlineQuery) OnlyX(ctx context.Context) *LastSeenOnline {
	node, err := lsoq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LastSeenOnline ID in the query.
// Returns a *NotSingularError when more than one LastSeenOnline ID is found.
// Returns a *NotFoundError when no entities are found.
func (lsoq *LastSeenOnlineQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = lsoq.Limit(2).IDs(setContextOp(ctx, lsoq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{lastseenonline.Label}
	default:
		err = &NotSingularError{lastseenonline.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (lsoq *LastSeenOnlineQuery) OnlyIDX(ctx context.Context) int {
	id, err := lsoq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LastSeenOnlines.
func (lsoq *LastSeenOnlineQuery) All(ctx context.Context) ([]*LastSeenOnline, error) {
	ctx = setContextOp(ctx, lsoq.ctx, ent.OpQueryAll)
	if err := lsoq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LastSeenOnline, *LastSeenOnlineQuery]()
	return withInterceptors[[]*LastSeenOnline](ctx, lsoq, qr, lsoq.inters)
}

// AllX is like All, but panics if an error occurs.
func (lsoq *LastSeenOnlineQuery) AllX(ctx context.Context) []*LastSeenOnline {
	nodes, err := lsoq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LastSeenOnline IDs.
func (lsoq *LastSeenOnlineQuery) IDs(ctx context.Context) (ids []int, err error) {
	if lsoq.ctx.Unique == nil && lsoq.path != nil {
		lsoq.Unique(true)
	}
	ctx = setContextOp(ctx, lsoq.ctx, ent.OpQueryIDs)
	if err = lsoq.Select(lastseenonline.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (lsoq *LastSeenOnlineQuery) IDsX(ctx context.Context) []int {
	ids, err := lsoq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (lsoq *LastSeenOnlineQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, lsoq.ctx, ent.OpQueryCount)
	if err := lsoq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, lsoq, querierCount[*LastSeenOnlineQuery](), lsoq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (lsoq *LastSeenOnlineQuery) CountX(ctx context.Context) int {
	count, err := lsoq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (lsoq *LastSeenOnlineQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, lsoq.ctx, ent.OpQueryExist)
	switch _, err := lsoq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (lsoq *LastSeenOnlineQuery) ExistX(ctx context.Context) bool {
	exist, err := lsoq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LastSeenOnlineQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (lsoq *LastSeenOnlineQuery) Clone() *LastSeenOnlineQuery {
	if lsoq == nil {
		return nil
	}
	return &LastSeenOnlineQuery{
		config:     lsoq.config,
		ctx:        lsoq.ctx.Clone(),
		order:      append([]lastseenonline.OrderOption{}, lsoq.order...),
		inters:     append([]Interceptor{}, lsoq.inters...),
		predicates: append([]predicate.LastSeenOnline{}, lsoq.predicates...),
		withUser:   lsoq.withUser.Clone(),
		// clone intermediate query.
		sql:  lsoq.sql.Clone(),
		path: lsoq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (lsoq *LastSeenOnlineQuery) WithUser(opts ...func(*UserQuery)) *LastSeenOnlineQuery {
	query := (&UserClient{config: lsoq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	lsoq.withUser = query
	return lsoq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SeenAt time.Time `json:"seen_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LastSeenOnline.Query().
//		GroupBy(lastseenonline.FieldSeenAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (lsoq *LastSeenOnlineQuery) GroupBy(field string, fields ...string) *LastSeenOnlineGroupBy {
	lsoq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LastSeenOnlineGroupBy{build: lsoq}
	grbuild.flds = &lsoq.ctx.Fields
	grbuild.label = lastseenonline.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SeenAt time.Time `json:"seen_at,omitempty"`
//	}
//
//	client.LastSeenOnline.Query().
//		Select(lastseenonline.FieldSeenAt).
//		Scan(ctx, &v)
func (lsoq *LastSeenOnlineQuery) Select(fields ...string) *LastSeenOnlineSelect {
	lsoq.ctx.Fields = append(lsoq.ctx.Fields, fields...)
	sbuild := &LastSeenOnlineSelect{LastSeenOnlineQuery: lsoq}
	sbuild.label = lastseenonline.Label
	sbuild.flds, sbuild.scan = &lsoq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LastSeenOnlineSelect configured with the given aggregations.
func (lsoq *LastSeenOnlineQuery) Aggregate(fns ...AggregateFunc) *LastSeenOnlineSelect {
	return lsoq.Select().Aggregate(fns...)
}

func (lsoq *LastSeenOnlineQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range lsoq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, lsoq); err != nil {
				return err
			}
		}
	}
	for _, f := range lsoq.ctx.Fields {
		if !lastseenonline.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if lsoq.path != nil {
		prev, err := lsoq.path(ctx)
		if err != nil {
			return err
		}
		lsoq.sql = prev
	}
	return nil
}

func (lsoq *LastSeenOnlineQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LastSeenOnline, error) {
	var (
		nodes       = []*LastSeenOnline{}
		withFKs     = lsoq.withFKs
		_spec       = lsoq.querySpec()
		loadedTypes = [1]bool{
			lsoq.withUser != nil,
		}
	)
	if lsoq.withUser != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, lastseenonline.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LastSeenOnline).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LastSeenOnline{config: lsoq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, lsoq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := lsoq.withUser; query != nil {
		if err := lsoq.loadUser(ctx, query, nodes, nil,
			func(n *LastSeenOnline, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (lsoq *LastSeenOnlineQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*LastSeenOnline, init func(*LastSeenOnline), assign func(*LastSeenOnline, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*LastSeenOnline)
	for i := range nodes {
		if nodes[i].user_last_seen_at == nil {
			continue
		}
		fk := *nodes[i].user_last_seen_at
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_last_seen_at" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (lsoq *LastSeenOnlineQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := lsoq.querySpec()
	_spec.Node.Columns = lsoq.ctx.Fields
	if len(lsoq.ctx.Fields) > 0 {
		_spec.Unique = lsoq.ctx.Unique != nil && *lsoq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, lsoq.driver, _spec)
}

func (lsoq *LastSeenOnlineQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(lastseenonline.Table, lastseenonline.Columns, sqlgraph.NewFieldSpec(lastseenonline.FieldID, field.TypeInt))
	_spec.From = lsoq.sql
	if unique := lsoq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if lsoq.path != nil {
		_spec.Unique = true
	}
	if fields := lsoq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, lastseenonline.FieldID)
		for i := range fields {
			if fields[i] != lastseenonline.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := lsoq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := lsoq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := lsoq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := lsoq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (lsoq *LastSeenOnlineQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(lsoq.driver.Dialect())
	t1 := builder.Table(lastseenonline.Table)
	columns := lsoq.ctx.Fields
	if len(columns) == 0 {
		columns = lastseenonline.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if lsoq.sql != nil {
		selector = lsoq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if lsoq.ctx.Unique != nil && *lsoq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range lsoq.predicates {
		p(selector)
	}
	for _, p := range lsoq.order {
		p(selector)
	}
	if offset := lsoq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := lsoq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LastSeenOnlineGroupBy is the group-by builder for LastSeenOnline entities.
type LastSeenOnlineGroupBy struct {
	selector
	build *LastSeenOnlineQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (lsogb *LastSeenOnlineGroupBy) Aggregate(fns ...AggregateFunc) *LastSeenOnlineGroupBy {
	lsogb.fns = append(lsogb.fns, fns...)
	return lsogb
}

// Scan applies the selector query and scans the result into the given value.
func (lsogb *LastSeenOnlineGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lsogb.build.ctx, ent.OpQueryGroupBy)
	if err := lsogb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LastSeenOnlineQuery, *LastSeenOnlineGroupBy](ctx, lsogb.build, lsogb, lsogb.build.inters, v)
}

func (lsogb *LastSeenOnlineGroupBy) sqlScan(ctx context.Context, root *LastSeenOnlineQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(lsogb.fns))
	for _, fn := range lsogb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*lsogb.flds)+len(lsogb.fns))
		for _, f := range *lsogb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*lsogb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lsogb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LastSeenOnlineSelect is the builder for selecting fields of LastSeenOnline entities.
type LastSeenOnlineSelect struct {
	*LastSeenOnlineQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (lsos *LastSeenOnlineSelect) Aggregate(fns ...AggregateFunc) *LastSeenOnlineSelect {
	lsos.fns = append(lsos.fns, fns...)
	return lsos
}

// Scan applies the selector query and scans the result into the given value.
func (lsos *LastSeenOnlineSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lsos.ctx, ent.OpQuerySelect)
	if err := lsos.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LastSeenOnlineQuery, *LastSeenOnlineSelect](ctx, lsos.LastSeenOnlineQuery, lsos, lsos.inters, v)
}

func (lsos *LastSeenOnlineSelect) sqlScan(ctx context.Context, root *LastSeenOnlineQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(lsos.fns))
	for _, fn := range lsos.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*lsos.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lsos.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
