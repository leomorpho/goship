// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/mikestefanello/pagoda/ent/predicate"
	"github.com/mikestefanello/pagoda/ent/profile"
	"github.com/mikestefanello/pagoda/ent/pwapushsubscription"
)

// PwaPushSubscriptionQuery is the builder for querying PwaPushSubscription entities.
type PwaPushSubscriptionQuery struct {
	config
	ctx         *QueryContext
	order       []pwapushsubscription.OrderOption
	inters      []Interceptor
	predicates  []predicate.PwaPushSubscription
	withProfile *ProfileQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PwaPushSubscriptionQuery builder.
func (ppsq *PwaPushSubscriptionQuery) Where(ps ...predicate.PwaPushSubscription) *PwaPushSubscriptionQuery {
	ppsq.predicates = append(ppsq.predicates, ps...)
	return ppsq
}

// Limit the number of records to be returned by this query.
func (ppsq *PwaPushSubscriptionQuery) Limit(limit int) *PwaPushSubscriptionQuery {
	ppsq.ctx.Limit = &limit
	return ppsq
}

// Offset to start from.
func (ppsq *PwaPushSubscriptionQuery) Offset(offset int) *PwaPushSubscriptionQuery {
	ppsq.ctx.Offset = &offset
	return ppsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ppsq *PwaPushSubscriptionQuery) Unique(unique bool) *PwaPushSubscriptionQuery {
	ppsq.ctx.Unique = &unique
	return ppsq
}

// Order specifies how the records should be ordered.
func (ppsq *PwaPushSubscriptionQuery) Order(o ...pwapushsubscription.OrderOption) *PwaPushSubscriptionQuery {
	ppsq.order = append(ppsq.order, o...)
	return ppsq
}

// QueryProfile chains the current query on the "profile" edge.
func (ppsq *PwaPushSubscriptionQuery) QueryProfile() *ProfileQuery {
	query := (&ProfileClient{config: ppsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ppsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ppsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(pwapushsubscription.Table, pwapushsubscription.FieldID, selector),
			sqlgraph.To(profile.Table, profile.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, pwapushsubscription.ProfileTable, pwapushsubscription.ProfileColumn),
		)
		fromU = sqlgraph.SetNeighbors(ppsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PwaPushSubscription entity from the query.
// Returns a *NotFoundError when no PwaPushSubscription was found.
func (ppsq *PwaPushSubscriptionQuery) First(ctx context.Context) (*PwaPushSubscription, error) {
	nodes, err := ppsq.Limit(1).All(setContextOp(ctx, ppsq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{pwapushsubscription.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ppsq *PwaPushSubscriptionQuery) FirstX(ctx context.Context) *PwaPushSubscription {
	node, err := ppsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PwaPushSubscription ID from the query.
// Returns a *NotFoundError when no PwaPushSubscription ID was found.
func (ppsq *PwaPushSubscriptionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ppsq.Limit(1).IDs(setContextOp(ctx, ppsq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{pwapushsubscription.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ppsq *PwaPushSubscriptionQuery) FirstIDX(ctx context.Context) int {
	id, err := ppsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PwaPushSubscription entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PwaPushSubscription entity is found.
// Returns a *NotFoundError when no PwaPushSubscription entities are found.
func (ppsq *PwaPushSubscriptionQuery) Only(ctx context.Context) (*PwaPushSubscription, error) {
	nodes, err := ppsq.Limit(2).All(setContextOp(ctx, ppsq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{pwapushsubscription.Label}
	default:
		return nil, &NotSingularError{pwapushsubscription.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ppsq *PwaPushSubscriptionQuery) OnlyX(ctx context.Context) *PwaPushSubscription {
	node, err := ppsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PwaPushSubscription ID in the query.
// Returns a *NotSingularError when more than one PwaPushSubscription ID is found.
// Returns a *NotFoundError when no entities are found.
func (ppsq *PwaPushSubscriptionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ppsq.Limit(2).IDs(setContextOp(ctx, ppsq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{pwapushsubscription.Label}
	default:
		err = &NotSingularError{pwapushsubscription.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ppsq *PwaPushSubscriptionQuery) OnlyIDX(ctx context.Context) int {
	id, err := ppsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PwaPushSubscriptions.
func (ppsq *PwaPushSubscriptionQuery) All(ctx context.Context) ([]*PwaPushSubscription, error) {
	ctx = setContextOp(ctx, ppsq.ctx, ent.OpQueryAll)
	if err := ppsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PwaPushSubscription, *PwaPushSubscriptionQuery]()
	return withInterceptors[[]*PwaPushSubscription](ctx, ppsq, qr, ppsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ppsq *PwaPushSubscriptionQuery) AllX(ctx context.Context) []*PwaPushSubscription {
	nodes, err := ppsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PwaPushSubscription IDs.
func (ppsq *PwaPushSubscriptionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ppsq.ctx.Unique == nil && ppsq.path != nil {
		ppsq.Unique(true)
	}
	ctx = setContextOp(ctx, ppsq.ctx, ent.OpQueryIDs)
	if err = ppsq.Select(pwapushsubscription.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ppsq *PwaPushSubscriptionQuery) IDsX(ctx context.Context) []int {
	ids, err := ppsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ppsq *PwaPushSubscriptionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ppsq.ctx, ent.OpQueryCount)
	if err := ppsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ppsq, querierCount[*PwaPushSubscriptionQuery](), ppsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ppsq *PwaPushSubscriptionQuery) CountX(ctx context.Context) int {
	count, err := ppsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ppsq *PwaPushSubscriptionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ppsq.ctx, ent.OpQueryExist)
	switch _, err := ppsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ppsq *PwaPushSubscriptionQuery) ExistX(ctx context.Context) bool {
	exist, err := ppsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PwaPushSubscriptionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ppsq *PwaPushSubscriptionQuery) Clone() *PwaPushSubscriptionQuery {
	if ppsq == nil {
		return nil
	}
	return &PwaPushSubscriptionQuery{
		config:      ppsq.config,
		ctx:         ppsq.ctx.Clone(),
		order:       append([]pwapushsubscription.OrderOption{}, ppsq.order...),
		inters:      append([]Interceptor{}, ppsq.inters...),
		predicates:  append([]predicate.PwaPushSubscription{}, ppsq.predicates...),
		withProfile: ppsq.withProfile.Clone(),
		// clone intermediate query.
		sql:  ppsq.sql.Clone(),
		path: ppsq.path,
	}
}

// WithProfile tells the query-builder to eager-load the nodes that are connected to
// the "profile" edge. The optional arguments are used to configure the query builder of the edge.
func (ppsq *PwaPushSubscriptionQuery) WithProfile(opts ...func(*ProfileQuery)) *PwaPushSubscriptionQuery {
	query := (&ProfileClient{config: ppsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ppsq.withProfile = query
	return ppsq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PwaPushSubscription.Query().
//		GroupBy(pwapushsubscription.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ppsq *PwaPushSubscriptionQuery) GroupBy(field string, fields ...string) *PwaPushSubscriptionGroupBy {
	ppsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PwaPushSubscriptionGroupBy{build: ppsq}
	grbuild.flds = &ppsq.ctx.Fields
	grbuild.label = pwapushsubscription.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.PwaPushSubscription.Query().
//		Select(pwapushsubscription.FieldCreatedAt).
//		Scan(ctx, &v)
func (ppsq *PwaPushSubscriptionQuery) Select(fields ...string) *PwaPushSubscriptionSelect {
	ppsq.ctx.Fields = append(ppsq.ctx.Fields, fields...)
	sbuild := &PwaPushSubscriptionSelect{PwaPushSubscriptionQuery: ppsq}
	sbuild.label = pwapushsubscription.Label
	sbuild.flds, sbuild.scan = &ppsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PwaPushSubscriptionSelect configured with the given aggregations.
func (ppsq *PwaPushSubscriptionQuery) Aggregate(fns ...AggregateFunc) *PwaPushSubscriptionSelect {
	return ppsq.Select().Aggregate(fns...)
}

func (ppsq *PwaPushSubscriptionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ppsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ppsq); err != nil {
				return err
			}
		}
	}
	for _, f := range ppsq.ctx.Fields {
		if !pwapushsubscription.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ppsq.path != nil {
		prev, err := ppsq.path(ctx)
		if err != nil {
			return err
		}
		ppsq.sql = prev
	}
	return nil
}

func (ppsq *PwaPushSubscriptionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PwaPushSubscription, error) {
	var (
		nodes       = []*PwaPushSubscription{}
		_spec       = ppsq.querySpec()
		loadedTypes = [1]bool{
			ppsq.withProfile != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PwaPushSubscription).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PwaPushSubscription{config: ppsq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ppsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ppsq.withProfile; query != nil {
		if err := ppsq.loadProfile(ctx, query, nodes, nil,
			func(n *PwaPushSubscription, e *Profile) { n.Edges.Profile = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ppsq *PwaPushSubscriptionQuery) loadProfile(ctx context.Context, query *ProfileQuery, nodes []*PwaPushSubscription, init func(*PwaPushSubscription), assign func(*PwaPushSubscription, *Profile)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*PwaPushSubscription)
	for i := range nodes {
		fk := nodes[i].ProfileID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(profile.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "profile_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ppsq *PwaPushSubscriptionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ppsq.querySpec()
	_spec.Node.Columns = ppsq.ctx.Fields
	if len(ppsq.ctx.Fields) > 0 {
		_spec.Unique = ppsq.ctx.Unique != nil && *ppsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ppsq.driver, _spec)
}

func (ppsq *PwaPushSubscriptionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(pwapushsubscription.Table, pwapushsubscription.Columns, sqlgraph.NewFieldSpec(pwapushsubscription.FieldID, field.TypeInt))
	_spec.From = ppsq.sql
	if unique := ppsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ppsq.path != nil {
		_spec.Unique = true
	}
	if fields := ppsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, pwapushsubscription.FieldID)
		for i := range fields {
			if fields[i] != pwapushsubscription.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if ppsq.withProfile != nil {
			_spec.Node.AddColumnOnce(pwapushsubscription.FieldProfileID)
		}
	}
	if ps := ppsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ppsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ppsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ppsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ppsq *PwaPushSubscriptionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ppsq.driver.Dialect())
	t1 := builder.Table(pwapushsubscription.Table)
	columns := ppsq.ctx.Fields
	if len(columns) == 0 {
		columns = pwapushsubscription.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ppsq.sql != nil {
		selector = ppsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ppsq.ctx.Unique != nil && *ppsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ppsq.predicates {
		p(selector)
	}
	for _, p := range ppsq.order {
		p(selector)
	}
	if offset := ppsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ppsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PwaPushSubscriptionGroupBy is the group-by builder for PwaPushSubscription entities.
type PwaPushSubscriptionGroupBy struct {
	selector
	build *PwaPushSubscriptionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ppsgb *PwaPushSubscriptionGroupBy) Aggregate(fns ...AggregateFunc) *PwaPushSubscriptionGroupBy {
	ppsgb.fns = append(ppsgb.fns, fns...)
	return ppsgb
}

// Scan applies the selector query and scans the result into the given value.
func (ppsgb *PwaPushSubscriptionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ppsgb.build.ctx, ent.OpQueryGroupBy)
	if err := ppsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PwaPushSubscriptionQuery, *PwaPushSubscriptionGroupBy](ctx, ppsgb.build, ppsgb, ppsgb.build.inters, v)
}

func (ppsgb *PwaPushSubscriptionGroupBy) sqlScan(ctx context.Context, root *PwaPushSubscriptionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ppsgb.fns))
	for _, fn := range ppsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ppsgb.flds)+len(ppsgb.fns))
		for _, f := range *ppsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ppsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ppsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PwaPushSubscriptionSelect is the builder for selecting fields of PwaPushSubscription entities.
type PwaPushSubscriptionSelect struct {
	*PwaPushSubscriptionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ppss *PwaPushSubscriptionSelect) Aggregate(fns ...AggregateFunc) *PwaPushSubscriptionSelect {
	ppss.fns = append(ppss.fns, fns...)
	return ppss
}

// Scan applies the selector query and scans the result into the given value.
func (ppss *PwaPushSubscriptionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ppss.ctx, ent.OpQuerySelect)
	if err := ppss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PwaPushSubscriptionQuery, *PwaPushSubscriptionSelect](ctx, ppss.PwaPushSubscriptionQuery, ppss, ppss.inters, v)
}

func (ppss *PwaPushSubscriptionSelect) sqlScan(ctx context.Context, root *PwaPushSubscriptionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ppss.fns))
	for _, fn := range ppss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ppss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ppss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
