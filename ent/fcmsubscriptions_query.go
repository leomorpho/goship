// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/mikestefanello/pagoda/ent/fcmsubscriptions"
	"github.com/mikestefanello/pagoda/ent/predicate"
	"github.com/mikestefanello/pagoda/ent/profile"
)

// FCMSubscriptionsQuery is the builder for querying FCMSubscriptions entities.
type FCMSubscriptionsQuery struct {
	config
	ctx         *QueryContext
	order       []fcmsubscriptions.OrderOption
	inters      []Interceptor
	predicates  []predicate.FCMSubscriptions
	withProfile *ProfileQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FCMSubscriptionsQuery builder.
func (fsq *FCMSubscriptionsQuery) Where(ps ...predicate.FCMSubscriptions) *FCMSubscriptionsQuery {
	fsq.predicates = append(fsq.predicates, ps...)
	return fsq
}

// Limit the number of records to be returned by this query.
func (fsq *FCMSubscriptionsQuery) Limit(limit int) *FCMSubscriptionsQuery {
	fsq.ctx.Limit = &limit
	return fsq
}

// Offset to start from.
func (fsq *FCMSubscriptionsQuery) Offset(offset int) *FCMSubscriptionsQuery {
	fsq.ctx.Offset = &offset
	return fsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fsq *FCMSubscriptionsQuery) Unique(unique bool) *FCMSubscriptionsQuery {
	fsq.ctx.Unique = &unique
	return fsq
}

// Order specifies how the records should be ordered.
func (fsq *FCMSubscriptionsQuery) Order(o ...fcmsubscriptions.OrderOption) *FCMSubscriptionsQuery {
	fsq.order = append(fsq.order, o...)
	return fsq
}

// QueryProfile chains the current query on the "profile" edge.
func (fsq *FCMSubscriptionsQuery) QueryProfile() *ProfileQuery {
	query := (&ProfileClient{config: fsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(fcmsubscriptions.Table, fcmsubscriptions.FieldID, selector),
			sqlgraph.To(profile.Table, profile.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, fcmsubscriptions.ProfileTable, fcmsubscriptions.ProfileColumn),
		)
		fromU = sqlgraph.SetNeighbors(fsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first FCMSubscriptions entity from the query.
// Returns a *NotFoundError when no FCMSubscriptions was found.
func (fsq *FCMSubscriptionsQuery) First(ctx context.Context) (*FCMSubscriptions, error) {
	nodes, err := fsq.Limit(1).All(setContextOp(ctx, fsq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{fcmsubscriptions.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fsq *FCMSubscriptionsQuery) FirstX(ctx context.Context) *FCMSubscriptions {
	node, err := fsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FCMSubscriptions ID from the query.
// Returns a *NotFoundError when no FCMSubscriptions ID was found.
func (fsq *FCMSubscriptionsQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fsq.Limit(1).IDs(setContextOp(ctx, fsq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{fcmsubscriptions.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fsq *FCMSubscriptionsQuery) FirstIDX(ctx context.Context) int {
	id, err := fsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FCMSubscriptions entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FCMSubscriptions entity is found.
// Returns a *NotFoundError when no FCMSubscriptions entities are found.
func (fsq *FCMSubscriptionsQuery) Only(ctx context.Context) (*FCMSubscriptions, error) {
	nodes, err := fsq.Limit(2).All(setContextOp(ctx, fsq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{fcmsubscriptions.Label}
	default:
		return nil, &NotSingularError{fcmsubscriptions.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fsq *FCMSubscriptionsQuery) OnlyX(ctx context.Context) *FCMSubscriptions {
	node, err := fsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FCMSubscriptions ID in the query.
// Returns a *NotSingularError when more than one FCMSubscriptions ID is found.
// Returns a *NotFoundError when no entities are found.
func (fsq *FCMSubscriptionsQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fsq.Limit(2).IDs(setContextOp(ctx, fsq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{fcmsubscriptions.Label}
	default:
		err = &NotSingularError{fcmsubscriptions.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fsq *FCMSubscriptionsQuery) OnlyIDX(ctx context.Context) int {
	id, err := fsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FCMSubscriptionsSlice.
func (fsq *FCMSubscriptionsQuery) All(ctx context.Context) ([]*FCMSubscriptions, error) {
	ctx = setContextOp(ctx, fsq.ctx, ent.OpQueryAll)
	if err := fsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FCMSubscriptions, *FCMSubscriptionsQuery]()
	return withInterceptors[[]*FCMSubscriptions](ctx, fsq, qr, fsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fsq *FCMSubscriptionsQuery) AllX(ctx context.Context) []*FCMSubscriptions {
	nodes, err := fsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FCMSubscriptions IDs.
func (fsq *FCMSubscriptionsQuery) IDs(ctx context.Context) (ids []int, err error) {
	if fsq.ctx.Unique == nil && fsq.path != nil {
		fsq.Unique(true)
	}
	ctx = setContextOp(ctx, fsq.ctx, ent.OpQueryIDs)
	if err = fsq.Select(fcmsubscriptions.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fsq *FCMSubscriptionsQuery) IDsX(ctx context.Context) []int {
	ids, err := fsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fsq *FCMSubscriptionsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fsq.ctx, ent.OpQueryCount)
	if err := fsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fsq, querierCount[*FCMSubscriptionsQuery](), fsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fsq *FCMSubscriptionsQuery) CountX(ctx context.Context) int {
	count, err := fsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fsq *FCMSubscriptionsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fsq.ctx, ent.OpQueryExist)
	switch _, err := fsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fsq *FCMSubscriptionsQuery) ExistX(ctx context.Context) bool {
	exist, err := fsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FCMSubscriptionsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fsq *FCMSubscriptionsQuery) Clone() *FCMSubscriptionsQuery {
	if fsq == nil {
		return nil
	}
	return &FCMSubscriptionsQuery{
		config:      fsq.config,
		ctx:         fsq.ctx.Clone(),
		order:       append([]fcmsubscriptions.OrderOption{}, fsq.order...),
		inters:      append([]Interceptor{}, fsq.inters...),
		predicates:  append([]predicate.FCMSubscriptions{}, fsq.predicates...),
		withProfile: fsq.withProfile.Clone(),
		// clone intermediate query.
		sql:  fsq.sql.Clone(),
		path: fsq.path,
	}
}

// WithProfile tells the query-builder to eager-load the nodes that are connected to
// the "profile" edge. The optional arguments are used to configure the query builder of the edge.
func (fsq *FCMSubscriptionsQuery) WithProfile(opts ...func(*ProfileQuery)) *FCMSubscriptionsQuery {
	query := (&ProfileClient{config: fsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fsq.withProfile = query
	return fsq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FCMSubscriptions.Query().
//		GroupBy(fcmsubscriptions.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (fsq *FCMSubscriptionsQuery) GroupBy(field string, fields ...string) *FCMSubscriptionsGroupBy {
	fsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FCMSubscriptionsGroupBy{build: fsq}
	grbuild.flds = &fsq.ctx.Fields
	grbuild.label = fcmsubscriptions.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.FCMSubscriptions.Query().
//		Select(fcmsubscriptions.FieldCreatedAt).
//		Scan(ctx, &v)
func (fsq *FCMSubscriptionsQuery) Select(fields ...string) *FCMSubscriptionsSelect {
	fsq.ctx.Fields = append(fsq.ctx.Fields, fields...)
	sbuild := &FCMSubscriptionsSelect{FCMSubscriptionsQuery: fsq}
	sbuild.label = fcmsubscriptions.Label
	sbuild.flds, sbuild.scan = &fsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FCMSubscriptionsSelect configured with the given aggregations.
func (fsq *FCMSubscriptionsQuery) Aggregate(fns ...AggregateFunc) *FCMSubscriptionsSelect {
	return fsq.Select().Aggregate(fns...)
}

func (fsq *FCMSubscriptionsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fsq); err != nil {
				return err
			}
		}
	}
	for _, f := range fsq.ctx.Fields {
		if !fcmsubscriptions.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fsq.path != nil {
		prev, err := fsq.path(ctx)
		if err != nil {
			return err
		}
		fsq.sql = prev
	}
	return nil
}

func (fsq *FCMSubscriptionsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FCMSubscriptions, error) {
	var (
		nodes       = []*FCMSubscriptions{}
		_spec       = fsq.querySpec()
		loadedTypes = [1]bool{
			fsq.withProfile != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FCMSubscriptions).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FCMSubscriptions{config: fsq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := fsq.withProfile; query != nil {
		if err := fsq.loadProfile(ctx, query, nodes, nil,
			func(n *FCMSubscriptions, e *Profile) { n.Edges.Profile = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (fsq *FCMSubscriptionsQuery) loadProfile(ctx context.Context, query *ProfileQuery, nodes []*FCMSubscriptions, init func(*FCMSubscriptions), assign func(*FCMSubscriptions, *Profile)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*FCMSubscriptions)
	for i := range nodes {
		fk := nodes[i].ProfileID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(profile.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "profile_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (fsq *FCMSubscriptionsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fsq.querySpec()
	_spec.Node.Columns = fsq.ctx.Fields
	if len(fsq.ctx.Fields) > 0 {
		_spec.Unique = fsq.ctx.Unique != nil && *fsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fsq.driver, _spec)
}

func (fsq *FCMSubscriptionsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(fcmsubscriptions.Table, fcmsubscriptions.Columns, sqlgraph.NewFieldSpec(fcmsubscriptions.FieldID, field.TypeInt))
	_spec.From = fsq.sql
	if unique := fsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fsq.path != nil {
		_spec.Unique = true
	}
	if fields := fsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, fcmsubscriptions.FieldID)
		for i := range fields {
			if fields[i] != fcmsubscriptions.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if fsq.withProfile != nil {
			_spec.Node.AddColumnOnce(fcmsubscriptions.FieldProfileID)
		}
	}
	if ps := fsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fsq *FCMSubscriptionsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fsq.driver.Dialect())
	t1 := builder.Table(fcmsubscriptions.Table)
	columns := fsq.ctx.Fields
	if len(columns) == 0 {
		columns = fcmsubscriptions.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fsq.sql != nil {
		selector = fsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fsq.ctx.Unique != nil && *fsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fsq.predicates {
		p(selector)
	}
	for _, p := range fsq.order {
		p(selector)
	}
	if offset := fsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FCMSubscriptionsGroupBy is the group-by builder for FCMSubscriptions entities.
type FCMSubscriptionsGroupBy struct {
	selector
	build *FCMSubscriptionsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fsgb *FCMSubscriptionsGroupBy) Aggregate(fns ...AggregateFunc) *FCMSubscriptionsGroupBy {
	fsgb.fns = append(fsgb.fns, fns...)
	return fsgb
}

// Scan applies the selector query and scans the result into the given value.
func (fsgb *FCMSubscriptionsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fsgb.build.ctx, ent.OpQueryGroupBy)
	if err := fsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FCMSubscriptionsQuery, *FCMSubscriptionsGroupBy](ctx, fsgb.build, fsgb, fsgb.build.inters, v)
}

func (fsgb *FCMSubscriptionsGroupBy) sqlScan(ctx context.Context, root *FCMSubscriptionsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fsgb.fns))
	for _, fn := range fsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fsgb.flds)+len(fsgb.fns))
		for _, f := range *fsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FCMSubscriptionsSelect is the builder for selecting fields of FCMSubscriptions entities.
type FCMSubscriptionsSelect struct {
	*FCMSubscriptionsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fss *FCMSubscriptionsSelect) Aggregate(fns ...AggregateFunc) *FCMSubscriptionsSelect {
	fss.fns = append(fss.fns, fns...)
	return fss
}

// Scan applies the selector query and scans the result into the given value.
func (fss *FCMSubscriptionsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fss.ctx, ent.OpQuerySelect)
	if err := fss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FCMSubscriptionsQuery, *FCMSubscriptionsSelect](ctx, fss.FCMSubscriptionsQuery, fss, fss.inters, v)
}

func (fss *FCMSubscriptionsSelect) sqlScan(ctx context.Context, root *FCMSubscriptionsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fss.fns))
	for _, fn := range fss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
