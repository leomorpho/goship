// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/mikestefanello/pagoda/ent/profile"
	"github.com/mikestefanello/pagoda/ent/sentemail"
)

// SentEmail is the model entity for the SentEmail schema.
type SentEmail struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Type holds the value of the "type" field.
	Type sentemail.Type `json:"type,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SentEmailQuery when eager-loading is set.
	Edges               SentEmailEdges `json:"edges"`
	profile_sent_emails *int
	selectValues        sql.SelectValues
}

// SentEmailEdges holds the relations/edges for other nodes in the graph.
type SentEmailEdges struct {
	// Profile holds the value of the profile edge.
	Profile *Profile `json:"profile,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ProfileOrErr returns the Profile value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SentEmailEdges) ProfileOrErr() (*Profile, error) {
	if e.Profile != nil {
		return e.Profile, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: profile.Label}
	}
	return nil, &NotLoadedError{edge: "profile"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SentEmail) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sentemail.FieldID:
			values[i] = new(sql.NullInt64)
		case sentemail.FieldType:
			values[i] = new(sql.NullString)
		case sentemail.FieldCreatedAt, sentemail.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case sentemail.ForeignKeys[0]: // profile_sent_emails
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SentEmail fields.
func (se *SentEmail) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sentemail.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			se.ID = int(value.Int64)
		case sentemail.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				se.CreatedAt = value.Time
			}
		case sentemail.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				se.UpdatedAt = value.Time
			}
		case sentemail.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				se.Type = sentemail.Type(value.String)
			}
		case sentemail.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field profile_sent_emails", value)
			} else if value.Valid {
				se.profile_sent_emails = new(int)
				*se.profile_sent_emails = int(value.Int64)
			}
		default:
			se.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SentEmail.
// This includes values selected through modifiers, order, etc.
func (se *SentEmail) Value(name string) (ent.Value, error) {
	return se.selectValues.Get(name)
}

// QueryProfile queries the "profile" edge of the SentEmail entity.
func (se *SentEmail) QueryProfile() *ProfileQuery {
	return NewSentEmailClient(se.config).QueryProfile(se)
}

// Update returns a builder for updating this SentEmail.
// Note that you need to call SentEmail.Unwrap() before calling this method if this SentEmail
// was returned from a transaction, and the transaction was committed or rolled back.
func (se *SentEmail) Update() *SentEmailUpdateOne {
	return NewSentEmailClient(se.config).UpdateOne(se)
}

// Unwrap unwraps the SentEmail entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (se *SentEmail) Unwrap() *SentEmail {
	_tx, ok := se.config.driver.(*txDriver)
	if !ok {
		panic("ent: SentEmail is not a transactional entity")
	}
	se.config.driver = _tx.drv
	return se
}

// String implements the fmt.Stringer.
func (se *SentEmail) String() string {
	var builder strings.Builder
	builder.WriteString("SentEmail(")
	builder.WriteString(fmt.Sprintf("id=%v, ", se.ID))
	builder.WriteString("created_at=")
	builder.WriteString(se.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(se.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", se.Type))
	builder.WriteByte(')')
	return builder.String()
}

// SentEmails is a parsable slice of SentEmail.
type SentEmails []*SentEmail
