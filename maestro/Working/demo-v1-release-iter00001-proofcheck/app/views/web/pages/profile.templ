package pages

// Renders: starter profile page.
templ Profile() {
	<section data-component="profile">
		<div data-slot="status">Profile</div>
		<h1>Profile</h1>
		<p>The default starter keeps the authenticated surface intentionally small: landing, auth entrypoints, home feed, and profile.</p>
	</section>
}
