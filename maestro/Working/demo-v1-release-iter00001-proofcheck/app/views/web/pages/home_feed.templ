package pages

// Renders: starter home feed page.
templ HomeFeed() {
	<section data-component="home-feed">
		<div data-slot="status">Authenticated Surface</div>
		<h1>Home Feed</h1>
		<p>Minimal authenticated home feed scaffold backed by auth and profile only.</p>
	</section>
}
