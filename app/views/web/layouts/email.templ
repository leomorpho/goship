package layouts

// Renders: standard email layout with centered content and consistent styling
// Route(s): all pages

templ Email(content templ.Component) {
	<div data-component="email">
		@content
	</div>
}
